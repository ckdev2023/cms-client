[project]
name = "mempalace"
version = "0.1.0"
requires-python = ">=3.9"
dependencies = ["chromadb==1.5.7", "mcp>=1.27.0,<2"]

[project.scripts]
mempalace = "mempalace.app:main"
mempalace-mcp = "mempalace.mcp_server:main"

[tool.pytest.ini_options]
testpaths = ["tests"]
