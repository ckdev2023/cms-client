/**
 * 迁移垫片（admin 拆分 B1）：本模块已移至 `__fixtures__/fixtures-create-bmv.ts`。
 *
 * 新代码请直接从 `__fixtures__/` 引用。本垫片在 B6 收口批随 codemod
 * 一并删除，届时旧路径由 ESLint no-restricted-imports 封禁，
 * 并限制仅测试可 import fixtures。
 */
export * from "./__fixtures__/fixtures-create-bmv";
