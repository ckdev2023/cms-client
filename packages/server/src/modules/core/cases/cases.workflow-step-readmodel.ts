/**
 * @deprecated 已迁至 `flow/workflow-step/workflowStepReadModel`（cases 拆分批次 S3）。
 *
 * 本垫片仅为迁移期保持既有 import 路径可用，收口批（S5）删除。
 * 新代码请直接从新路径导入。
 */
export * from "./flow/workflow-step/workflowStepReadModel";
