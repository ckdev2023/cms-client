/**
 * 运营使用手册章节内容（结构化）。
 *
 * 每个 chapter 字段：
 *   - id: 锚点 / 文件名前缀
 *   - title: 显示标题
 *   - summary: 章节摘要（h1 下面那段）
 *   - body: Markdown 字符串数组，会按顺序拼接
 *   - sections: 子节，每个子节可包含 screenshot id、字段表、流程图等
 */

export const COVER = {
  productName: "Gyosei OS · 行政書士 SaaS 管理后台",
  manualTitle: "运营使用手册（Admin 后台全功能）",
  edition: "P0 首版",
  audience: "公司内部运营 / 客户成功 / 实施工程师",
  generatedAt: new Date().toISOString().slice(0, 10),
};

export const CHAPTERS = [
  // =====================================================================
  // 第 1 章：系统准入与全局界面
  // =====================================================================
  {
    id: "ch01",
    title: "第 1 章 · 系统准入与全局界面",
    summary:
      "本章介绍后台登录、全局布局、左侧导航分组、角色与权限模型，是后续所有操作的前置基础。",
    sections: [
      {
        id: "ch01-1",
        heading: "1.1 后台登录",
        screenshot: "01-login",
        screenshotCaption: "图 1-1 · 后台登录页（左侧业务介绍 / 右侧登录表单）",
        markdown: `
**入口**：\`http://<部署域名>/admin\`（本地开发：\`http://127.0.0.1:5173/#/login\`）。

| 字段 | 说明 |
|---|---|
| **邮箱** | 由系统管理员开通的工作邮箱，必须与 \`users.email\` 完全匹配（系统按 lower-case 存储） |
| **密码** | 首次由管理员发放临时密码，登录后必须立即在「系统设置 → 成员管理」中改密 |
| **登录后跳转** | 默认跳到「仪表盘」；若有 \`?redirect=\` 参数则跳到指定页面 |

**登录失败常见原因**：

1. **邮箱大小写**：后端会全部转 lower-case 比对；输入时大小写无所谓，但白名单导出时统一以小写为准。
2. **账号被停用**：\`status != 'active'\` 时一律 401，错误提示与密码错误一样，需查表确认。
3. **服务端没起来**：浏览器看到「请求失败」，先用 \`curl http://<域>/health\` 自检。

> 完整的账号查找与重置流程见 **附录 C · 账号异常排查**。
        `,
      },
      {
        id: "ch01-2",
        heading: "1.2 全局界面布局",
        screenshot: "02-dashboard",
        screenshotCaption: "图 1-2 · 全局布局：左侧导航 + 顶部工具条 + 主区域",
        markdown: `
所有业务页面共用同一套 Shell（\`packages/admin/src/shell/\`）：

- **左侧导航**：分组展开式，按业务领域分组——
  - **工作台**：仪表盘
  - **业务**：咨询会话 / 客户 / 案件 / 任务与提醒
  - **内容**：资料中心
  - **财务**：收费与财务
  - **系统**：系统设置（仅 owner / manager 可见）
- **顶部工具条**：
  - 全局搜索框（搜索：客户 / 案件 / 资料 / 文书，⌘K 唤起）
  - 语言切换（简体中文 / 日本語 / English）
  - 快捷新建：「新建咨询」「新建案件」
  - 用户菜单：当前账号 / 退出登录
- **面包屑导航**：每页主区域顶部，点击可向上回溯

> **小贴士**：左侧导航的可见项是按当前用户权限自动过滤的；如果你看不到某个菜单，先检查角色与权限——见 **第 9 章 · 系统设置**。
        `,
      },
      {
        id: "ch01-3",
        heading: "1.3 角色与权限模型",
        markdown: `
P0 首版采用「角色 + 权限码 + 个人覆盖」三层模型：

| 角色 | 默认能力 | 适用对象 |
|---|---|---|
| **owner** | 全部权限，含系统设置写权限 | 事务所主办人 / 系统初始管理员 |
| **manager** | 业务全权 + 用户与组管理 | 部门主管 / 资深行政书士 |
| **staff** | 案件、客户、资料、任务读写；不可改系统配置 | 一般行政书士 / 事务员 |
| **viewer** | 仅查看，全部模块只读 | 外部审计 / 临时查阅 |

**关键权限码**（节选，完整列表见「角色管理」面板）：

| 权限码 | 含义 |
|---|---|
| \`case.view\` / \`case.create\` / \`case.write\` | 案件查看 / 创建 / 编辑 |
| \`customer.view\` / \`customer.write\` | 客户档案查看 / 编辑 |
| \`document.write\` | 资料登记、补正、豁免 |
| \`billing.write\` | 收费登记与回款 |
| \`user.manage\` / \`group.manage\` | 成员 / 分组管理 |
| \`permission.override\` | 给特定成员单独加 / 减权限 |
| \`settings.write\` | 写组织级配置（可见性 / 资料根目录等） |

**特别说明**：

- 角色是基础，**个人覆盖（user_permission_overrides）** 可以单独给某人加 / 减权限，常用于「让某 staff 临时具备收费写权限」。
- 「**组（Group）**」决定数据可见范围——一个用户可属于多个组，但只能有一个 \`primary_group\`；组隔离规则见 **第 9.4 节 · 可见性配置**。
        `,
      },
    ],
  },

  // =====================================================================
  // 第 2 章：工作台（仪表盘）
  // =====================================================================
  {
    id: "ch02",
    title: "第 2 章 · 工作台（仪表盘）",
    summary:
      "仪表盘是登录后的默认入口，把每天最该处理的事——今日待办、已逾期、客户回信、风险案件——一屏聚合。",
    sections: [
      {
        id: "ch02-1",
        heading: "2.1 仪表盘整体结构",
        screenshot: "02-dashboard",
        screenshotCaption: "图 2-1 · 仪表盘全貌（自上而下：身份卡 → 快捷动作 → 数据卡 → 优先级 → 风险列表）",
        markdown: `
仪表盘由 5 个区块自上而下排列：

1. **身份卡片**：今天好「\${当前用户名}·\${角色}」+ 角色与组徽章；左侧大字标题，右侧角色 / 组下拉切换（仅多组用户）
2. **快捷动作**（4 张大卡）：「新建咨询线索」「新建客户」「新建案件」「立刻办理 / 到账」
3. **时段切换**：7 天 / 30 天 ；快捷链接「一键收回今日」「进入今日工作台」「查看试点 / 上线状态」
4. **数据卡区**（5 张数据卡）：今日待办 / 已逾期 / 客户回信 / 持续交事件 / 风险事件
5. **TOP PRIORITY · 今日待办列表**：当天必须处理的具体动作，每条卡片含：
   - 任务标题（如「顾客との初回面談を確認」）
   - 关联案件：编号 + 客户名 + 模板（认定 4 个月）
   - 状态 / 优先级 / 负责人
   - 操作：「查看」「标记完成」
6. **DEADLINES · 已逾期到期日**：未来 30 天内到期的所有结构化期限
7. **SUBMISSION · 持续交事件**：在押期、补正回复期等
8. **RISKS · 风险事件**：欠款、阻断校验、缺件超期等
        `,
      },
      {
        id: "ch02-2",
        heading: "2.2 数据卡含义",
        markdown: `
| 数据卡 | 计算口径 | 点击跳转 |
|---|---|---|
| **今日待办** | \`tasks.due_at\` 在今天范围内 + \`status='pending'\` | 任务列表，筛选「今日到期」 |
| **已逾期** | \`tasks.due_at < now()\` AND status='pending' | 任务列表，筛选「已逾期」 |
| **客户回信** | 最近 7 天 inbound 消息且未标记已读 | 会话列表，筛选「未读 inbound」 |
| **持续交事件** | 处于 S7「已提交审理中」的案件 + 待补正 | 案件列表，筛选「stage=S7 OR has_amendment」 |
| **风险事件** | 欠款 / 阻断校验未消除 / 缺件超期 | 风险列表（按风险类型分组） |

> **每天看仪表盘的标准动作**：①「今日待办」必须当天清空；②「已逾期」必须当天给出说明或推进；③「客户回信」每天处理；④「风险事件」每周一次清单复核。
        `,
      },
    ],
  },

  // =====================================================================
  // 第 3 章：咨询线索与会话
  // =====================================================================
  {
    id: "ch03",
    title: "第 3 章 · 咨询线索与会话",
    summary:
      "咨询线索是案件的入口：从客户首次联系到决定签约前的所有沟通都在这里管理；签约后线索会转化为正式案件。会话则承载与客户的来往消息与备注。",
    sections: [
      {
        id: "ch03-1",
        heading: "3.1 线索状态机",
        markdown: `
P0 首版线索状态机（\`leads.status\`）：

\`\`\`
新规咨询(initial_inquiry) → 跟进中(in_progress) → 已签约(signed) → 已转化案件(converted_case)
                                                  ↘ 已流失(lost)
\`\`\`

| 状态 | 进入条件 | 典型动作 |
|---|---|---|
| **initial_inquiry** | 新规创建（手动 / 由 portal 表单 / 由会话生成） | 分配负责人、首次回复 |
| **in_progress** | 已有至少一次双向沟通 | 安排面谈、收集需求 |
| **signed** | 已签订委任合同 | 等待转化为案件（人工触发） |
| **converted_case** | 已生成案件（不可逆） | 在案件视图后续推进 |
| **lost** | 客户放弃 / 我方不接 | 关闭线索；保留沟通记录用于复盘 |
        `,
      },
      {
        id: "ch03-2",
        heading: "3.2 咨询线索列表",
        screenshot: "03-leads-list",
        screenshotCaption: "图 3-2 · 咨询线索列表（含状态筛选、来源筛选、负责人筛选）",
        markdown: `
**字段说明**：

| 列 | 含义 |
|---|---|
| **咨询人 / 编号** | 客户姓名 / 公司名 + 自动生成的 LEAD-YYYYMMDD-XXX 编号 |
| **联系方式 / 咨询渠道** | 邮箱 / 电话 + 渠道（portal / 介绍 / 主动来信 等） |
| **当前状态** | 线索状态机（见 §3.1） |
| **负责人 / 分组** | 当前指派的行政书士 / 所属业务组 |
| **来源 / 介绍人** | 来自哪个渠道，是否有介绍人（影响后续分账） |
| **最近联系** | 最近一次双向沟通的时间 |

**典型筛选场景**：

- 「我的 + 状态：跟进中」 → 我手上还在谈但没签约的
- 「全部 + 状态：已签约 + 来源：portal」 → 上周通过官网线索签约的客户
- 「负责人：未分配 + 状态：新规咨询」 → 抢单池：等待分配的新线索
        `,
      },
      {
        id: "ch03-3",
        heading: "3.3 会话列表与详情",
        screenshots: [
          { id: "03-conversations-list", caption: "图 3-3a · 会话列表" },
          { id: "03-conversation-detail", caption: "图 3-3b · 会话详情：消息流 + 备注 + 关联线索" },
        ],
        markdown: `
**会话** 与 **线索** 的关系：每个 portal 来源的线索自动绑定一个 **会话**；同一客户可以有多个会话（不同时期、不同主题）。

**会话详情页能做什么**：

1. 查看与客户的消息流（系统消息 / inbound 客户消息 / outbound 我方回复）
2. 添加内部备注（仅事务所内可见，不发给客户）
3. 标记重要消息（pin）
4. 把会话内容快速「转线索 → 转案件」

> P0 首版**不**做：自动回复、聊天机器人、模板消息批发。所有 outbound 消息都需要人工撰写。
        `,
      },
    ],
  },

  // =====================================================================
  // 第 4 章：客户管理
  // =====================================================================
  {
    id: "ch04",
    title: "第 4 章 · 客户管理",
    summary:
      "客户是案件的实体主体。一个客户可以有多个案件（如：本人在留 + 家族滞在 + 续签）。客户档案承载基础信息、关联人、沟通记录与操作日志。",
    sections: [
      {
        id: "ch04-1",
        heading: "4.1 客户列表",
        screenshot: "04-customers-list",
        screenshotCaption: "图 4-1 · 客户列表（按累计案件、活跃案件、最近建案排序）",
        markdown: `
**列表能力**：

- 按 \`name_ja\` / \`name_en\` / \`identifier\` / \`phone\` / \`email\` 模糊搜索
- 按所属分组、负责人、客户类型（个人 / 法人）筛选
- 列：识别名 / 累计案件数 / 活跃案件数 / 归档数 / 最近建案 / 客户负责人

**新建客户的两条路径**：

1. 顶部「+ 新建客户」按钮 → 直接录入
2. 从已签约的咨询线索「转化」时自动创建（推荐路径，能保留来源链）
        `,
      },
      {
        id: "ch04-2",
        heading: "4.2 客户详情 — 基础信息",
        screenshot: "04-customer-detail",
        screenshotCaption: "图 4-2 · 客户详情：基础信息 Tab（含识别名、姓名、国籍、所在地等）",
        markdown: `
**关键字段**：

| 字段组 | 字段 | 说明 |
|---|---|---|
| **识别** | 识别名（对内显示）/ 姓名（法定）/ 假名（片假名） | 识别名用于内部检索；姓名严格按法定文档；假名供入管文书使用 |
| **法律身份** | 国籍 / 性别 / 生日 | 影响在留资格类型与必须材料 |
| **联系** | 电话 / 邮箱 / 所在地 / 来源渠道 | 邮箱可触发提醒邮件，缺失时提醒走 SMS / 信件 |
| **关系** | 所属分组 / 客户负责人 / 介绍人 | 决定可见性与分账 |
| **业务** | 签证类型 / 介绍人姓名 | 仅当客户在某签证类型有续签 / 变更需求时填写 |
| **附件** | 写真 / 头像 / 备注 | 备注可写「优先打电话不发邮件」等沟通偏好 |

**编辑约束**：

- 修改客户的法定姓名 / 国籍 / 生日 → 自动写入审计日志（保留旧值）
- 删除客户 → P0 不允许直接删除；只能停用（status='inactive'）
        `,
      },
      {
        id: "ch04-3",
        heading: "4.3 客户详情 — 关联案件",
        screenshot: "04-customer-detail-cases",
        screenshotCaption: "图 4-3 · 客户详情：关联案件 Tab",
        markdown: `
该 Tab 列出此客户的所有案件，含：

- 案件编号 / 标题 / 类型（家族滞在 / 技人国 等）
- 当前阶段（S1–S9）
- 负责人
- 最近变更时间

**点击案件行 → 直接跳到案件详情**。从客户视角追案件是最常用的横向跳转路径。
        `,
      },
      {
        id: "ch04-4",
        heading: "4.4 客户详情 — 沟通记录",
        screenshot: "04-customer-detail-comms",
        screenshotCaption: "图 4-4 · 客户详情：沟通记录 Tab（聚合所有渠道的通讯）",
        markdown: `
聚合显示与该客户**所有**会话、所有渠道的消息流（按时间倒序），并按渠道（邮件 / 系统通知 / portal 消息 / 备注）打标签。

> 这是「这个客户跟我们说过什么 / 我们跟他说过什么」的唯一权威视图。
        `,
      },
      {
        id: "ch04-5",
        heading: "4.5 客户详情 — 操作日志",
        screenshot: "04-customer-detail-log",
        screenshotCaption: "图 4-5 · 客户详情：操作日志 Tab",
        markdown: `
列出对这个客户档案的所有变更，每条含：

- 操作时间 / 操作人
- 操作类型（创建 / 修改 / 停用 / 关联案件 等）
- 变更详情（diff 形式）

**用途**：客户投诉「你们改了我的资料」时，给客户出示完整修改史。
        `,
      },
    ],
  },

  // =====================================================================
  // 第 5 章：案件管理（最重要章节）
  // =====================================================================
  {
    id: "ch05",
    title: "第 5 章 · 案件管理（核心）",
    summary:
      "案件是 P0 首版的核心业务对象。一个案件 = 一个客户 + 一个签证类型 + 一次完整的从建档到归档的生命周期。本章按「全生命周期 → 列表 → 创建 → 详情各 Tab」的顺序讲解。",
    sections: [
      {
        id: "ch05-1",
        heading: "5.1 案件全生命周期（S1–S9）",
        mermaid: `
flowchart LR
  S1[S1 已建档] --> S2[S2 资料收集中]
  S2 --> S3[S3 资料审核中]
  S3 -->|Gate-A 通过| S4[S4 文书制作中]
  S4 -->|Gate-B 通过| S5[S5 待校验]
  S5 --> S6[S6 待提交]
  S6 -->|Gate-C 提交| S7[S7 已提交审理中]
  S7 -->|登记结果| S8[S8 已出结果]
  S8 -->|结案归档| S9[S9 已归档]
  S7 -.补正循环.-> S4
        `,
        markdown: `
P0 首版主链路：**咨询录入 → 客户建档 → 案件创建 → 资料收集与审核 → 文书生成与定稿 → 提交前校验 → 正式提交 → 审理跟踪 → 出结果 → 归档**。

**三道关键 Gate**（阻断式校验）：

| Gate | 触发点 | 通过后效果 | 阻断原因示例 |
|---|---|---|---|
| **Gate-A** | S3 → S4 或首次生成关键文书前 | 进入文书制作；自动生成 / 更新文书任务 | 必备资料缺失、客户身份未确认 |
| **Gate-B** | S4 → S5 或主动「执行检验」 | 生成 \`ValidationRun\` 校验报告 | 文书必填字段缺失、字段间逻辑冲突 |
| **Gate-C** | 点击「正式提交」 | 生成 \`SubmissionPackage\`，阶段 → S7 | 校验未通过、双人复核未完成（如启用） |

**P0 / P1 边界**：
- P0 只用「管理层阶段 S1–S9」
- P1 才启用「业务子步骤 \`CaseWorkflowStep\`」（如 COE_SENT / VISA_APPLYING）
- P0 续签提醒以**手动 Reminder 为主**
        `,
      },
      {
        id: "ch05-2",
        heading: "5.2 案件列表",
        screenshot: "05-cases-list",
        screenshotCaption: "图 5-2 · 案件列表（含进行中数量、即将到期、欠款金额等汇总卡）",
        markdown: `
**顶部 4 张汇总卡**：

| 卡 | 口径 |
|---|---|
| **进行中案件** | \`status NOT IN ('archived','closed')\` |
| **接受验证** | 通过 Gate-B 但还没到 S6 的案件 |
| **即将到期** | 任意结构化期限在 30 天内 |
| **待续金额** | 所有未结清账单合计 |

**Tabs**：「我的案件」/「未结」/「案件」（全部）

**筛选维度**：

- 全部阶段（S1–S9）
- 全部负责人
- 全部分组
- 全部风险（欠款 / 阻断校验 / 期限超期）
- 全部模板（家族滞在 / 技人国 / 经营管理 / R6 试点 等）

**列字段**：案件 / 阶段 / 申请人 / 类型 / 负责人 / 到期日 / 持续 / 检验 / 风险

> 风险列「正常」「待检验」「风险」等是综合标签，鼠标悬停可看具体哪类风险。
        `,
      },
      {
        id: "ch05-3",
        heading: "5.3 新建案件",
        screenshot: "05-case-create",
        screenshotCaption: "图 5-3 · 新建案件页（左侧模板选择，右侧字段录入）",
        markdown: `
**两步走**：

1. **选择模板**：P0 首版预置「家族滞在」「技人国」两种模板（演示环境还有「经营管理」「R6 试点」）。模板决定：
   - 默认资料清单
   - 默认文书清单
   - 默认任务节奏
   - 默认期限规则
2. **录入基础信息**：
   - 案件标题（自动从模板 + 申请人姓名生成；可手动改）
   - 主申请人（必填，从客户库选；不能新建）
   - 分组与负责人（默认当前用户的主组）
   - 预计提交日期（影响倒推任务期限）
   - 备注

**点击「创建」后**：

- 系统创建 case 行 + 自动建档 = S1
- 按模板生成资料清单 \`document_items\`
- 按模板生成文书占位 \`forms\`
- 按模板生成第一批任务（如「与客户初回面谈」）

> **不要**在没有客户档案的情况下新建案件——必须先创建客户。
        `,
      },
      {
        id: "ch05-4",
        heading: "5.4 案件详情 — 概览",
        screenshot: "05-case-detail",
        screenshotCaption: "图 5-4 · 案件详情概览：当前进度 / 资料完成率 / 团队 / 阻断风险摘要",
        markdown: `
案件详情页顶部固定区域始终展示：

- **面包屑**：仪表盘 → 业务 → 案件 → CASE-XXX
- **标题行**：案件标题（如「家族滞在 — 田中太郎」）+ 状态徽章「S1·已建档」「· 准备中」
- **副信息行**：申请人 | 负责人 | 分组旗
- **右侧操作**：「编辑信息」「导出 ZIP」「状态流转」

**Tabs**（共 10 个）：概览 / 提交前检查 / 资料清单 \`x/n\` / 任务 / 基础信息 / 文书 / 期限 / 收费 / 沟通记录 / 日志

**「概览」展示什么**：

1. 4 张大卡：当前办案进度 / 预计截止日期 / 资料完成率（带进度条）/ 财务状况
2. 按提供方完成率：事务所内部 / 扶养者 · 保证人（横向进度条）
3. 下一关键动作：「资料管理」「执行检验」（一键跳到对应 Tab）
4. 近期动态（最近 5 条变更，链接到日志 Tab）
5. 阻断与风险摘要：阻断项数 / 期限警告 / 检验状态
6. 案件团队卡片
7. 提交前校验卡片（直达 Tab）
        `,
      },
      {
        id: "ch05-5",
        heading: "5.5 案件详情 — 提交前检查（Gate-B / Gate-C 入口）",
        screenshot: "05-case-detail-validation",
        screenshotCaption: "图 5-5 · 提交前检查 Tab（校验报告 + 提交包历史 + 双人复核 + 欠款风险确认）",
        markdown: `
该 Tab 是 **Gate-B 和 Gate-C 的统一入口**。

**4 个面板**：

1. **提交前检查**：
   - 顶部「下一步：推进至 Sx」+「重新检查」按钮
   - 校验状态：「校验通过，无阻断项」/「有 N 项阻断」
   - 失败时展示阻断列表（哪个字段、哪个文书）
2. **提交包（历史快照）**：
   - 列出所有历史 \`SubmissionPackage\`，每条 = 一次正式提交的不可变快照
   - 提交包含：所有引用的资料 / 文书 / 校验报告
   - 点击查看 → 可以打开该提交时刻的完整状态
3. **双人复核**：
   - P0 默认不强制；事务所或案件模板开启时才作为 \`S5 → S6\` 的附加门槛
   - 「发起复核」→ 指派另一名 staff/manager 审核
4. **欠款风险确认**：
   - 案件已欠款时，提交前必须先「登记欠款风险确认」（书面承诺与客户的还款约定）
   - P0 不阻断提交；P1 可配置成阻断

**底部说明**：「下签后处理（COE / 海外贴签 / 返签结果）」——P0 首版**未启用**，UI 占位提示「当前案件未到该阶段」。
        `,
      },
      {
        id: "ch05-6",
        heading: "5.6 案件详情 — 资料清单（S2 / S3 主战场）",
        screenshot: "05-case-detail-documents",
        screenshotCaption: "图 5-6 · 资料清单 Tab：按提供方分组 + 单条状态 + 操作按钮",
        markdown: `
**结构**：按「提供方」分组（事务所内部 / 扶养者 · 保证人 / 主申请人 等），每组内每行 = 一项资料。

**资料项状态机**：

\`\`\`
未登记(pending) → 已登记待审(waiting) → 审核中(reviewing) → 已通过(approved)
                                                       ↘ 退回补正(rejected) → 重新登记
                                              ↘ 标记无需提供(waived)
\`\`\`

**单行可用操作**（取决于状态）：

| 状态 | 可用操作 |
|---|---|
| **未登记** | 「催办」「登记资料」「引用既有版本」「标记无需提供」 |
| **待审** | 「审核通过」「退回补正」 |
| **退回补正** | 客户重新提交 → 回到待审 |
| **已通过** | 「查看详情」「重新登记」 |
| **无需提供（豁免）** | 「取消豁免」 |

**关键概念**：

- **引用既有版本** = 客户在另一个案件已经提交过同类资料（如护照复印件），可以直接引用而不需要客户重传
- **标记无需提供（豁免）** = 该项资料对当前案件不适用（如「身元保証書」对没有保证人的案件）；豁免必须填理由
- **多版本不覆盖**：客户重新提交时旧版本保留，便于回溯

> 顶部进度条 \`x/n 已通过审核\` 是**资料完成率**的来源；这个数字也会显示在概览 Tab。
        `,
      },
      {
        id: "ch05-7",
        heading: "5.7 案件详情 — 文书（S4）",
        screenshot: "05-case-detail-forms",
        screenshotCaption: "图 5-7 · 文书 Tab",
        markdown: `
文书 Tab 列出该案件应提交的所有官方文书（在留資格認定証明書交付申請書 等），每条含：

- 文书模板名（含语言）
- 当前版本号 / 是否已定稿
- 关联字段值预览
- 操作：「编辑」「定稿」「下载 PDF」「替换为新模板」

**P0 首版采用模板驱动**：

1. 案件创建时按模板预先生成文书占位
2. 资料和案件信息自动填入文书字段
3. 用户在编辑器中补全 / 调整后「定稿」
4. 定稿后版本不可改，需要再改要新建版本

> 文书的字段填充冲突 / 缺失会在 **Gate-B（提交前检查）** 阶段统一报出。
        `,
      },
      {
        id: "ch05-8",
        heading: "5.8 案件详情 — 任务",
        screenshot: "05-case-detail-tasks",
        screenshotCaption: "图 5-8 · 任务 Tab",
        markdown: `
**任务（Task）** vs **提醒（Reminder）**：

- **任务**：必须完成的具体动作（如「与客户面谈」「上传基础資料」），有截止时间和完成态
- **提醒**：到时间通知，不强制完成（如「在留期限到期前 180 天通知」）

**任务种类**（P0 首版）：

| 种类 | 用途 |
|---|---|
| \`client_contact\` | 与客户的初回面谈、回访 |
| \`document_follow_up\` | 资料催办 |
| \`form_review\` | 文书审核 |
| \`submission_prep\` | 提交准备 |
| \`amendment_handle\` | 补正处理 |

**操作**：

- 「标记完成」：完成日期 = now()
- 「重新打开」：仅 24 小时内可撤回
- 「转交」：把任务转给另一名 staff
- 「调整截止日」
        `,
      },
      {
        id: "ch05-9",
        heading: "5.9 案件详情 — 期限",
        screenshot: "05-case-detail-deadlines",
        screenshotCaption: "图 5-9 · 期限 Tab",
        markdown: `
所有结构化期限的统一视图：

- **签证有效期 / 在留期限**：用于自动生成续签提醒（P1 才批量生成；P0 手动）
- **补正回复期**：入管发出补正后必须在 N 天内回复
- **预计提交日期**：内部目标日
- **追加调查回复期**：入管追加询问后的回复期

**到期前的提醒**：到期前 \`X\` 天会在仪表盘「即将到期」与任务列表自动出现。\`X\` 由 reminder_schedule_blueprint 决定（P0 默认值 180/90/60/30/14/7/1 天）。
        `,
      },
      {
        id: "ch05-10",
        heading: "5.10 案件详情 — 收费",
        screenshot: "05-case-detail-billing",
        screenshotCaption: "图 5-10 · 收费 Tab",
        markdown: `
**收费记录字段**：

| 字段 | 说明 |
|---|---|
| 应收金额 | 合同约定的总额 |
| 已收 | 已经入账的金额 |
| 待收 | 应收 - 已收 |
| 下一收费节点 | 如「案件提交后」「结果出后」 |
| 回款状态 | 未到期 / 即将到期 / 已收齐 / 部分到期 / 逾期 |
| 收费风险摘要 | 「欠款风险已确认」「未确认」 |

**操作**：

- 「登记回款」：录入实收金额、收款方式、收款日
- 「调整账单」：仅在客户未付款前可改
- 「登记欠款风险确认」：在金额逾期时，与客户书面约定还款计划

> 收费节点用于**提醒与风险提示**，**不默认阻断**提交动作（P0 策略）。
        `,
      },
      {
        id: "ch05-11",
        heading: "5.11 案件详情 — 日志",
        screenshot: "05-case-detail-log",
        screenshotCaption: "图 5-11 · 日志 Tab：完整审计时间线",
        markdown: `
所有对该案件的变更按时间倒序排列，每条含：

- 时间戳
- 操作人
- 事件类型（如 \`stage.transition\` / \`document.approved\` / \`submission.created\`）
- 详情（diff 或 JSON 摘要）

**主要事件类型**（节选）：

| 事件 | 含义 |
|---|---|
| \`case.created\` | 案件创建 |
| \`stage.transition\` | 阶段流转（S1→S2 等） |
| \`document.uploaded\` / \`document.approved\` / \`document.rejected\` / \`document.waived\` | 资料登记 / 审核动作 |
| \`form.drafted\` / \`form.finalized\` | 文书拟定 / 定稿 |
| \`validation.run\` | 校验执行（含通过 / 失败、阻断项） |
| \`submission.created\` | 提交包生成（Gate-C 通过） |
| \`amendment.received\` / \`amendment.replied\` | 补正接收 / 回复 |
| \`result.recorded\` | 结果登记（出 COE / 拒绝 / 部分通过） |
| \`case.archived\` | 归档 |

> 日志是**只追加**，不可修改；任何「修改了某条记录」也会以新事件追加。
        `,
      },
    ],
  },

  // =====================================================================
  // 第 6 章：资料中心
  // =====================================================================
  {
    id: "ch06",
    title: "第 6 章 · 资料中心",
    summary:
      "资料中心是跨案件的资料聚合视图：找一件资料、跨案件复用、批量催办，都在这里。",
    sections: [
      {
        id: "ch06-1",
        heading: "6.1 资料中心总览",
        screenshot: "06-documents-list",
        screenshotCaption: "图 6-1 · 资料中心：跨案件资料汇总（含状态统计）",
        markdown: `
**4 张统计卡**：

| 卡 | 口径 |
|---|---|
| **待审核** | 状态 = 待审（事务所内的等审项） |
| **缺件 / 待登记** | 状态 = pending（客户还没交） |
| **过期** | 已通过但有效期已过 |
| **共享路径过期风险** | 引用了即将过期资料的活跃案件数 |

**列表字段**：

| 列 | 说明 |
|---|---|
| 资料项名称 | 名称 + 内部 key（如 \`employment_certificate\`） |
| 所属案件 | 关联的案件编号 |
| 提供方 | 事务所内部 / 主申请人 / 扶养者 · 保证人 |
| 状态 | 待登记 / 待审 / 待提交 / 已通过 / 退回补正 / 无需提供 |
| 截止日 | 资料的内部截止 |
| 最后变更 | 最近一次状态变化时间 |
| 半检引擎 | 自动校验状态（P1 启用） |

**操作**：「+ 登记资料」「+ 手动添加」（添加非清单内的临时资料）。

**典型用途**：
- 找「赵客户的最新护照复印件在哪」
- 批量催办「所有 5 天没交基础资料的客户」
- 周清单：所有「过期」的资料项是否已经替换为新版
        `,
      },
    ],
  },

  // =====================================================================
  // 第 7 章：任务与提醒
  // =====================================================================
  {
    id: "ch07",
    title: "第 7 章 · 任务与提醒",
    summary:
      "每个事务员的「今天该干什么」。仪表盘的「今日待办」「已逾期」点进来就是这里。",
    sections: [
      {
        id: "ch07-1",
        heading: "7.1 任务与提醒列表",
        screenshot: "07-tasks-list",
        screenshotCaption: "图 7-1 · 任务与提醒列表（4 张统计卡 + 任务表格）",
        markdown: `
**4 张统计卡**：

| 卡 | 口径 |
|---|---|
| **待处理任务** | 还没完成的所有任务 |
| **今日到期** | 今天必须完成的 |
| **已逾期** | 已过截止时间还没完成的 |
| **提醒日志** | 续签等定时提醒最近生成的条数 |

**列表字段**：任务（标题 + 类型 key）/ 案件 · 责任人 / 截止时间 / 状态 / 优先级 / 操作

**关键操作**：

- **标记完成**：把任务关掉，仪表盘相应数字 -1
- **页面说明** 折叠区：解释每种任务的来源（系统自动生成 / 模板生成 / 手动添加）
        `,
      },
      {
        id: "ch07-2",
        heading: "7.2 续签提醒生成机制（P0 vs P1）",
        markdown: `
**P0 首版**：续签提醒 **手动添加**——管理员判断后在客户档案 / 案件期限 Tab 里添加「在留期限到期日」，系统按内置 reminder_schedule_blueprint 生成提醒任务。

**P1**：通过 \`ResidencePeriod + reminder_schedule_blueprint\` 批量生成续签提醒；管理员只需录入「在留期限到期日」即可。

> 默认提醒节奏：到期前 **180 / 90 / 60 / 30 / 14 / 7 / 1** 天各一次。
        `,
      },
    ],
  },

  // =====================================================================
  // 第 8 章：收费与财务
  // =====================================================================
  {
    id: "ch08",
    title: "第 8 章 · 收费与财务",
    summary:
      "事务所的收钱视图：所有案件的应收 / 已收 / 欠款一屏，便于财务每周对账。",
    sections: [
      {
        id: "ch08-1",
        heading: "8.1 收费与财务总览",
        screenshot: "08-billing-list",
        screenshotCaption: "图 8-1 · 收费与财务总览（4 张汇总卡 + 案件收费列表）",
        markdown: `
**4 张汇总卡**：

| 卡 | 口径 |
|---|---|
| **总应收 (JPY)** | 所有未结清账单合计 |
| **当日收 (JPY)** | 今天入账金额 |
| **总待收登款 (JPY)** | 等待收款的余额 |
| **逾期欠款 (JPY)** | 超过应收日仍未收的金额 |

**Tabs**：「案件收费列表」/「回款流水记录」

**列表字段**：

| 列 | 说明 |
|---|---|
| 案件名称 | 案件 + 编号 |
| 客户 | 主申请人 |
| 所属分组 | 决定可见性 |
| 应收 (¥) / 已收 (¥) / 待收 (¥) | 三件套 |
| 下一收费节点 | 如「案件提交后」「无（已结清）」 |
| 回款状态 | 已结清 / 部分到期 / 未到期 / 案件提醒 / 案件已逾期 / 已逾期 |
| 收费风险摘要 | 风险标签 |
| 操作 | 「登记回款」 |

**典型用途**：
- 财务月底：拉「当日收」「总待收」对账
- 主办：每周一次「逾期欠款」清单复核，给客户发催收邮件
        `,
      },
    ],
  },

  // =====================================================================
  // 第 9 章：系统设置
  // =====================================================================
  {
    id: "ch09",
    title: "第 9 章 · 系统设置",
    summary:
      "组织级配置入口：分组、成员、角色、可见性、本地资料根目录。仅 owner / manager 可见。",
    sections: [
      {
        id: "ch09-1",
        heading: "9.1 分组管理",
        screenshot: "09-settings-groups",
        screenshotCaption: "图 9-1 · 分组管理：分组列表 + 状态 + 关联案件数",
        markdown: `
**分组（Group）**是数据可见范围的单位。一个事务所可以建多个组（按地域、按业务线、按部门），每个用户至少属于一个组。

**操作**：

- 「+ 新建分组」：填名称，自动 active
- 「停用」：把分组停用，组员仍可登录但看不到该组数据
- 状态筛选：全部 / 启用 / 停用

**字段**：

| 字段 | 说明 |
|---|---|
| 分组名称 | 内部展示名 |
| 状态 | active / disabled |
| 创建时间 | 自动 |
| 活跃案件 | 该组拥有的活跃案件数 |
| 成员 | 组内人数 |
        `,
      },
      {
        id: "ch09-2",
        heading: "9.2 成员管理",
        screenshot: "09-settings-members",
        screenshotCaption: "图 9-2 · 成员管理：用户列表 + 角色 + 状态 + 邀请",
        markdown: `
**操作**：

- 「+ 邀请成员」：填邮箱、角色、加入分组，系统发送邀请邮件 + 临时密码
- 「修改角色」：弹窗选 owner / manager / staff / viewer
- 「重置密码」：生成新临时密码（一次性密码，弹窗显示后必须立即抄给当事人）
- 「停用」：status='disabled'，立即不能登录
- 「权限覆盖」：抽屉式打开，给该成员单独加 / 减权限码

**字段**：邮箱 / 显示名 / 角色 / 状态 / 主分组 / 最近登录

**密码安全**：

- 临时密码不能写进 \`.env\`，弹窗关闭后无法再次查看
- 用户首次登录后**必须**自行修改密码
- 详细的应急重置流程见 **附录 C · 账号异常排查**
        `,
      },
      {
        id: "ch09-3",
        heading: "9.3 角色与权限",
        screenshot: "09-settings-roles",
        screenshotCaption: "图 9-3 · 角色与权限：左侧角色列表 + 右侧详情（含权限矩阵）",
        markdown: `
**4 个系统角色**（不可删除）：owner / manager / staff / viewer。

**右侧详情**：

- 角色基本信息（code / 显示名 / 描述）
- 权限矩阵：按业务模块分组的权限码（勾选 = 该角色拥有该权限）
- 持有该角色的成员列表

**自定义角色**（P1）：可以基于系统角色克隆出自定义角色，调整权限矩阵。P0 首版**只用系统角色**。

**权限覆盖（user_permission_overrides）**：

- 比角色更细：可以单独给某成员加权限或减权限
- 用例：「事务员李四临时具备收费写权限」
- 入口：成员管理列表 → 某成员 → 「权限覆盖」抽屉
        `,
      },
      {
        id: "ch09-4",
        heading: "9.4 可见性配置",
        screenshot: "09-settings-visibility",
        screenshotCaption: "图 9-4 · 可见性配置：组隔离规则",
        markdown: `
**默认组隔离规则**：

- 一个用户**只能看到**他所属组的客户、案件、资料
- owner / manager 可以看所有组（除非主动配置例外）
- 跨组协作：必须把目标用户加到对应组

**可配置项**：

- **强制单组主**：每个用户只能有一个 \`primary_group\`（默认开启）
- **允许跨组查询客户**：默认关闭；开启后所有用户可搜到所有组的客户名（但不能进详情）
- **公共资料模板组**：哪个组的「资料 / 文书模板」对所有组可见
        `,
      },
      {
        id: "ch09-5",
        heading: "9.5 本地资料根目录",
        screenshot: "09-settings-storage",
        screenshotCaption: "图 9-5 · 本地资料根目录：文件落盘路径配置",
        markdown: `
**用途**：所有客户上传 / 内部登记的资料文件实际存储在 \`<rootPath>/{orgId}/files/{caseId}/...\`。

**字段**：

| 字段 | 说明 |
|---|---|
| 根路径（rootPath） | 服务器上的绝对路径，如 \`/data/cms/files\` |
| 标签（rootLabel） | 给运维看的友好名 |

**注意事项**：

- **不要中途改路径**：改了之后老文件不会自动迁移，需要运维手动 \`mv\`
- 路径必须对运行 server / worker 进程的用户**可读可写**
- 建议放在独立挂载点（NFS / 对象存储挂载），便于备份
- 容量监控：建议每周查 disk usage，超 80% 报警
        `,
      },
    ],
  },

  // =====================================================================
  // 附录
  // =====================================================================
  {
    id: "appendix-a",
    title: "附录 A · SOP 与运营节奏",
    summary: "把日常 / 周 / 月运营动作模板化，避免漏掉关键节点。",
    sections: [
      {
        id: "appx-a-1",
        heading: "A.1 每日 SOP（事务员）",
        markdown: `
| 时间 | 动作 | 入口 |
|---|---|---|
| 09:00 | 看仪表盘「今日待办」「已逾期」「客户回信」三张卡 | 仪表盘 |
| 09:30 | 处理「已逾期」中的所有任务（要么完成，要么留备注） | 任务与提醒 |
| 10:00 | 处理「客户回信」：每条至少回一次或转线索 / 案件 | 会话列表 |
| 12:00 / 17:30 | 把「今日待办」清空 | 任务与提醒 |
| 18:00 | 风险事件复核：欠款 / 阻断校验 | 仪表盘 / 收费 |
        `,
      },
      {
        id: "appx-a-2",
        heading: "A.2 每周 SOP（主办行政书士）",
        markdown: `
| 周节点 | 动作 |
|---|---|
| 周一 09:30 | 团队站会：过一遍每个 staff 的「我的案件」「逾期」 |
| 周三 | 跨案件资料中心：盘点所有「过期」资料是否已替换 |
| 周五 17:00 | 收费与财务：拉「逾期欠款」清单，列下周催收名单 |
| 周五 18:00 | 提交前检查：所有 S5 / S6 案件，跑一遍 Gate-B 校验 |
        `,
      },
      {
        id: "appx-a-3",
        heading: "A.3 每月 SOP（管理者）",
        markdown: `
- 当月归档案件复盘（S9 列表）：统计本月通过 / 拒绝 / 补正比例
- 财务对账：「回款流水记录」与银行流水对照
- 续签预警：未来 6 个月到期的在留 = 下月起手客户名单
- 系统设置：检查停用成员是否还有未交接案件
        `,
      },
    ],
  },
  {
    id: "appendix-b",
    title: "附录 B · 常见问题 FAQ",
    summary: "运营人员最常遇到的 12 个问题。",
    sections: [
      {
        id: "appx-b-1",
        heading: "B.1 操作类",
        markdown: `
**Q1：登录后侧边栏看不到「系统设置」？**
A：你的角色是 staff / viewer。系统设置只对 owner / manager 可见。如需用，请管理员把你的角色改成 manager。

**Q2：新建案件时，下拉里找不到客户？**
A：先去「客户」模块创建客户档案，再回来新建案件。或：从「咨询线索」的「转化」按钮一步走完。

**Q3：客户上传的资料看不到？**
A：检查 3 件事——
1. 你和案件不在同一个组（可见性规则）
2. 资料状态在「待登记」（客户没传），不在「待审」
3. 案件是否已归档（归档后所有资料只读）

**Q4：怎么把一个资料标记为「不需要提供」？**
A：案件详情 → 资料清单 Tab → 找到对应资料行 → 点「标记无需提供」→ 填豁免理由（必填）。

**Q5：提交前检查失败，但我看不到具体错在哪？**
A：在「提交前检查」Tab 顶部点「重新检查」，等待校验完成（通常 5-15 秒），失败项会在卡片下方列出。

**Q6：客户付了一半钱，怎么登记？**
A：案件详情 → 收费 Tab → 「登记回款」→ 填实收金额 + 收款日 + 备注。

**Q7：怎么把案件转给另一个事务员？**
A：案件详情 → 编辑信息 → 改「负责人」字段 → 保存。系统自动给新负责人分发任务通知。
        `,
      },
      {
        id: "appx-b-2",
        heading: "B.2 数据与显示类",
        markdown: `
**Q8：仪表盘的数据卡数字不对？**
A：先点页面右上角「刷新」按钮（或 ⌘R）。如果还是不对，检查：
- 是否切换了组 / 时段（7 天 / 30 天）
- 是否过去 5 分钟有大批量变更（缓存可能滞后 60 秒）
- 给运维：拉 \`/health/cache\` 看缓存命中率

**Q9：列表搜索找不到一个明明存在的客户？**
A：搜索是模糊匹配 \`name_ja\` / \`name_en\` / \`identifier\` / \`phone\` / \`email\`。
- 如果客户在另一个组，且没开启「跨组查询客户」→ 搜不到
- 如果只搜到一半字符，确认搜索框输入是否完整
- 客户已停用 → 默认筛选会过滤掉，需手动展开「全部状态」

**Q10：案件详情打不开（白屏 / 报错）？**
A：浏览器 F12 打开 Console，看红色报错截图发运维。常见原因：
- 案件 ID 不存在（链接是别人发的旧链接）
- 网络断了（左下角红条）
- 浏览器缓存过期，⌘ + Shift + R 强刷
        `,
      },
      {
        id: "appx-b-3",
        heading: "B.3 业务流程类",
        markdown: `
**Q11：怎么处理入管的补正通知？**
A：流程——
1. 案件详情 → 期限 Tab → 添加「补正回复期」+ 入管要求的截止日
2. 期限 Tab 自动生成「补正处理任务」
3. 资料清单 Tab → 把要重新提交的资料行点「退回补正」让客户重传
4. 重传后审核通过 → 跑 Gate-B 重新校验 → 重新生成提交包

**Q12：想批量给一组客户发提醒怎么办？**
A：P0 首版**没有**批量消息功能。当前手动方案：
- 资料中心 → 筛选「过期 / 缺件超期」→ 导出 CSV
- 用 CSV 在邮件客户端 / LINE 批量群发（人工操作）

> P1 计划：批量提醒模板 + 一键发送（详见 \`docs/gyoseishoshi_saas_md/P1\`）。
        `,
      },
    ],
  },
  {
    id: "appendix-c",
    title: "附录 C · 账号异常排查",
    summary: "登录失败、密码遗忘、账号被停用、应急重置——压缩自 release/ADMIN-USERS.md。详细 SQL 见原文档。",
    sections: [
      {
        id: "appx-c-1",
        heading: "C.1 登录失败排查清单",
        markdown: `
按顺序排查：

1. **账号是否存在 + active**
   \`\`\`sql
   select id, status, role_id from users where lower(email) = lower('xxx@example.com');
   \`\`\`
   \`status\` 必须 = \`active\`。
2. **密码 hash 是否完整**
   \`\`\`sql
   select length(password_hash), substring(password_hash,1,7) from users where lower(email) = lower('xxx');
   \`\`\`
   长度约 100~110；前缀必须是 \`scrypt$\`。
3. **密码本身错**：让用户重输；如确认遗忘 → 走 §C.2 重置。
4. **后端日志**：\`bash scripts/logs.sh api | grep -i 'auth\\|login'\` 看具体拒绝原因。

> 后端把所有失败原因（密码错、status 非 active、role 解析失败、hash 缺失）统一返回 \`Invalid email or password\`，前端文案分不出来——必须查表或查日志。
        `,
      },
      {
        id: "appx-c-2",
        heading: "C.2 重置密码（运维操作）",
        markdown: `
**首选：用 \`initLocalAdmin.ts\` 脚本重置（覆盖式 upsert）**

\`\`\`bash
ADMIN_INIT_EMAIL='admin@example.com' \\
ADMIN_INIT_PASSWORD='YourStrongPassword!2026' \\
$COMPOSE exec \\
  -e ADMIN_INIT_EMAIL \\
  -e ADMIN_INIT_PASSWORD \\
  api node --import tsx src/scripts/initLocalAdmin.ts
\`\`\`

成功后立即登录 → 后台改密码（这是临时密码）。

**应急：在另一台机器算 hash → 直接 UPDATE**

\`\`\`bash
node -e '
  const c = require("crypto");
  const password = process.argv[1];
  const salt = c.randomBytes(16).toString("base64url");
  c.scrypt(password, Buffer.from(salt, "base64url"), 64, (err, key) => {
    if (err) throw err;
    console.log("scrypt$" + salt + "$" + key.toString("base64url"));
  });
' 'YourStrongPassword!2026'
\`\`\`

把输出的 hash UPDATE 进 users 表。

> **重要**：每次重置后立即清掉 shell history 中的明文密码。完整的安全清理流程见 \`release/ADMIN-USERS.md §6\`。
        `,
      },
      {
        id: "appx-c-3",
        heading: "C.3 停用 / 启用账号",
        markdown: `
推荐用后台 UI（系统设置 → 成员管理）。SQL 应急：

\`\`\`sql
-- 停用
update users set status = 'disabled', updated_at = now()
 where lower(email) = lower('alice@example.com');

-- 启用
update users set status = 'active', updated_at = now()
 where lower(email) = lower('alice@example.com');
\`\`\`

> 停用后用户立即不能登录；他名下的案件会保留，需要主办手动转交。
        `,
      },
    ],
  },
  {
    id: "appendix-d",
    title: "附录 D · 术语表",
    summary: "30 个核心术语速查（详细定义见 docs/gyoseishoshi_saas_md/P0/08-术语表.md）。",
    sections: [
      {
        id: "appx-d-1",
        heading: "D.1 业务对象",
        markdown: `
| 术语 | 中文 | 含义 |
|---|---|---|
| Lead | 咨询线索 | 客户首次联系到决定签约前的全部线索；签约后转 Case |
| Customer | 客户 | 案件主体；可有多个 Case |
| CaseParty | 案件参与方 | 申请人 / 扶养者 / 保证人 / 介绍人 |
| Case | 案件 | 一个签证类型 + 一次完整生命周期 |
| Stage (S1–S9) | 阶段 | 案件管理层阶段，驱动工作台与默认任务 |
| Gate (A/B/C) | 门槛 | 阶段间的阻断式校验 |
| ValidationRun | 校验报告 | Gate-B 触发的字段 / 文书校验快照 |
| SubmissionPackage | 提交包 | Gate-C 通过后生成的不可变快照 |
| DocumentItem | 资料项 | 案件资料清单的单元 |
| DocumentRequirement | 资料模板项 | 模板上预定义的资料行 |
| Form | 文书 | 官方提交文书的模板实例 |
| Task | 任务 | 必须完成的具体动作 |
| Reminder | 提醒 | 到时间通知，不强制完成 |
| ResidencePeriod | 在留期限 | 续签提醒的时间锚点 |
| Group | 分组 | 数据可见范围单位 |
        `,
      },
      {
        id: "appx-d-2",
        heading: "D.2 角色 / 权限",
        markdown: `
| 术语 | 含义 |
|---|---|
| Role | 系统角色（owner / manager / staff / viewer） |
| Permission Code | 细粒度权限码（如 \`case.create\`） |
| User Permission Override | 个人权限覆盖（在角色之上加 / 减权限） |
| Primary Group | 用户的主分组（每人有且仅有 1 个） |
        `,
      },
      {
        id: "appx-d-3",
        heading: "D.3 状态枚举（节选）",
        markdown: `
| 模块 | 状态 |
|---|---|
| 案件 stage | S1 / S2 / S3 / S4 / S5 / S6 / S7 / S8 / S9 |
| 资料项 status | pending / waiting / reviewing / approved / rejected / waived |
| 任务 status | pending / completed / cancelled |
| 线索 status | initial_inquiry / in_progress / signed / converted_case / lost |
| 收费 settle_status | not_due / partial / paid / overdue / risk_confirmed |
| 用户 status | active / disabled |
        `,
      },
    ],
  },
];
