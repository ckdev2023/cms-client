# 06 数据模型设计

本章以最新需求的主链路为基准：咨询转化 → 建档 → 收资料/催办 → 文书 → 校验/复核 → 提交快照 → 审理跟踪 → 结果归档 → 续签提醒 → 回款 → 留痕。

## 1. 设计原则

- 以案件为中心，但把“阶段（Stage）”“门槛（Gate）”“快照（Submission Package）”拆开落地，便于自动化与审计
- 客户资料可复用：一个客户可多案；家庭/多案件场景可共享材料“版本”，但引用必须可锁定
- 资料项是“要求”，附件是“版本”：每次上传生成新版本，不覆盖历史；提交包只能引用具体版本
- 文书与模板版本不可变：导出/提交引用固定版本，保证历史可追溯
- 客户可见域与内部域隔离：沟通、资料备注、校验结果需明确可见性与字段范围
- 全量关键事件可留痕：状态流转、资料审核、文书定稿、校验/复核、提交包生成、收费节点确认等

## 1.1 跨实体通用字段与治理约束

- 作为 SaaS 系统，核心业务主实体应统一具备 `org_id/workspace_id` 一类租户隔离字段，避免后期补租户成本过高
- 高频写入实体建议统一具备 `created_by`、`updated_by`、`created_at`、`updated_at`，必要时增加 `deleted_at` 或 `archived_at` 支持软删除/归档
- 涉及客户门户展示的实体应带可见性字段（如 `visible_scope`、`client_visible_flag`），不要依赖前端页面自行约束
- 编号类字段（lead_no、customer_no、case_no、submission_no）建议按组织内唯一生成，并保留人工可读格式

## 1.2 索引与唯一约束建议

- 唯一约束：`lead_no`、`customer_no`、`case_no`、`submission_no` 建议在组织内唯一；`company_no` 仅在启用企业主数据时需要组织内唯一
- 检索索引：客户的 `group_id/owner_user_id`、案件的 `group_id/stage/principal_user_id/next_action_due_at`、资料项的 `status/due_date`、任务的 `assignee_user_id/status/due_at`、提醒的 `recipient_id/remind_at/send_status`
- 去重辅助：联系方式类字段建议保存规范化值（如 `normalized_phone`、`normalized_email`），配合姓名/生日进行重复提示
- 快照查询：提交包与附件版本建议按 `case_id + submitted_at`、`requirement_id + version_no` 建索引，保证时间线与追溯查询效率

## 1.3 P0 策略落模原则

- 模型层需要预留“可开启但不强制”的能力，例如双人复核、客户门户、外部提醒，不应把这些能力写死为所有案件必选
- 会影响业务策略的门槛建议显式建模，例如收费阻断策略、复核开关、模板启用范围，避免把规则散落在前端或人工约定里
- P0 仅要求支撑 2 个高频模板闭环：家族滞在、工作签证（技人国）；模板模型先保证可扩展，不要求首版即覆盖全量签证类型
- 若业务侧不建立“企业客户”主数据（真实作业以个人建档为主），P0 仍应把 group（归属团队/项目组）作为强依赖维度，支撑检索、分工、看板与可见性口径；工作类案件的雇主/受入机构信息可先作为 Case 字段沉淀

---

## 2. 核心实体总览（MVP）

- Lead（咨询线索）
- Group（归属团队/项目组）
- Customer（个人客户）
- Company（企业客户，P1/P2 可选：如需企业主数据、企业侧协作与批量能力）
- ContactPerson（联系人/企业担当，P1/P2 可选）
- Case（案件）
- CaseParty（案件关联人：主申请人/扶养者/保证人/家属/企业联系人等）
- CaseStageHistory（案件阶段流转记录）
- CaseTemplate（案件模板：生成资料清单/默认任务/默认校验规则集）
- DocumentRequirement（资料项：材料要求）
- DocumentFileVersion（资料附件版本：每次上传生成一条不可变版本）
- CommunicationLog（沟通记录：内部/客户可见分域）
- Task（任务：推动案件流转的驱动单元）
- CaseDeadline（期限：在留到期/补件截止/预约日等统一管理）
- Template（文书模板：版本化）
- GeneratedDocumentVersion（生成文书版本：初稿/审核中/定稿等）
- ValidationRun（提交前校验运行：结果分层，含硬性阻断/软性提示）
- ReviewRecord（双人复核：通过/驳回原因）
- SubmissionPackage（提交包：一次对外提交快照）
- SubmissionPackageItem（提交包锁定引用：资料版本/文书版本/关键字段快照）
- BillingPlan（收费计划/节点）
- PaymentRecord（回款记录）
- Reminder/Notification（提醒与触达记录：内部/客户）
- AuditLog（审计日志：全对象通用）
- User / Role（内部用户与角色；客户侧用户建议独立为 ClientUser）

---

## 3. 实体设计建议

## 3.0 Group（归属团队/项目组）

用途：用于表达“客户与案件归属哪个团队/项目组”的业务维度，支撑内部协作分工、列表筛选、看板统计与可见性边界。Group 是组织内的治理对象，不等同于随手可打的标签。

### 关键字段
- id
- group_no（可选：组织内可读编号）
- name（团队/项目组名称）
- active_flag
- created_at
- updated_at

## 3.1 Lead（咨询线索）

用途：承接“未签约/未立案”的早期信息与跟进动作，转化时再生成客户与案件，避免污染主数据。

### 关键字段
- id
- lead_no
- name
- phone
- email
- source_channel（来源/介绍人/渠道）
- intended_case_type（意向业务类型）
- status（新咨询/跟进中/待签约/已签约/已转案件/已流失）
- next_action
- next_follow_up_at
- quote_amount（可选）
- note
- owner_user_id
- converted_customer_id（可选）
- converted_case_id（可选）
- lost_reason（可选）
- created_at
- updated_at

---

## 3.2 Customer（个人客户）

### 关键字段
- id
- customer_no
- display_name（对内识别名）
- name_cn
- name_jp
- name_en
- name_kana（フリガナ）
- gender
- nationality
- birth_date
- phone
- email
- address
- passport_no（可选）
- residence_card_no（可选）
- current_status_of_residence（可选）
- residence_expiry_date（可选）
- profile_photo_url（可选）
- referrer_name（可选）
- group_id（归属团队/项目组：引用 Group，P0 强依赖）
- note（可检索）
- language_preference（用于消息/门户）
- owner_user_id
- created_at
- updated_at

---

## 3.3 Company（企业客户）

### 关键字段
- id
- company_no
- display_name（对内识别名/简称）
- company_name
- company_name_kana（フリガナ）
- corporate_number（法人番号）
- established_date（可选）
- fiscal_year_end（可选）
- head_office_address（本店住所）
- tax_office（所属税務署，可选）
- contact_phone（可选）
- contact_email（可选）
- referrer_name（可选）
- group_id
- note
- owner_user_id
- created_at
- updated_at

---

## 3.4 ContactPerson（联系人/企业担当）

### 关键字段
- id
- company_id（可选）
- customer_id（可选：用于“个人关系人”）
- name
- name_kana（可选）
- role_title（可选）
- relation_type（法定代表人/人事担当/紧急联系人等，可选）
- phone（可选）
- email（可选）
- preferred_language（可选）
- created_at
- updated_at

---

## 3.5 Case（案件）

### 关键字段
- id
- case_no
- case_name
- source_lead_id（可选：来源线索）
- case_type（家族滞在/工作/经营管理等）
- application_type（认定/变更/更新等）
- customer_id（主申请人客户，可选：部分企业案可能以企业为主，但仍应能绑定主申请人）
- group_id（案件归属团队/项目组：默认继承 Customer.group_id；如发生转组需留痕）
- company_id（可选：如启用企业主数据，P1/P2 引用 Company）
- employer_name（可选：工作类案件的雇主/受入机构名称；P0 可先以字段沉淀）
- employer_corporate_number（可选：法人番号）
- employer_address（可选）
- employer_contact_name（可选）
- employer_contact_phone（可选）
- stage（见 3.6）
- priority
- risk_level
- principal_user_id（负责人）
- assistant_user_id（协作/助理，可选）
- source_channel（来源：从线索/渠道带入）
- signed_at（签约时间，可选）
- result_outcome（pending/approved/rejected/withdrawn 等，可选）
- close_reason（可选）
- archive_reason（可选）
- archived_at（归档时间，可选）
- next_action（用于工作台聚合）
- next_action_due_at（可选）
- created_at
- updated_at

### 建议阶段枚举（与 05 对齐）
- S1 已建档
- S2 资料收集中
- S3 资料审核中
- S4 文书制作中
- S5 待校验
- S6 待提交
- S7 已提交审理中
- S8 已出结果
- S9 已归档

---

## 3.6 CaseStageHistory（阶段流转记录）

用途：阶段不是纯审计字段，需要可查询、可看板统计、可时间线展示。

### 关键字段
- id
- case_id
- from_stage（可选：首条为空）
- to_stage
- reason（可选）
- changed_by
- changed_at

---

## 3.7 CaseParty（案件关联人）

用途：覆盖家庭办理与关键关系门槛（主申请人/扶养者/保证人等）。

### 关键字段
- id
- case_id
- party_role（主申请人/扶养者/保证人/配偶/子女/企业担当等）
- customer_id（可选）
- contact_person_id（可选）
- is_primary（例如主申请人）
- required_flag（是否为关键关系，影响硬性阻断）
- created_at
- updated_at

---

## 3.8 CaseTemplate（案件模板）

用途：按案件类型/申请类型生成资料清单、默认任务、校验规则集与文书模板建议。

### 关键字段
- id
- template_name
- case_type
- application_type（可选）
- requirement_blueprint（JSON：资料项定义、提供方、是否必交、默认截止策略等）
- default_tasks_blueprint（JSON，可选）
- validation_ruleset_ref（JSON：校验项集合与严重性）
- review_required_flag（是否要求双人复核；默认可为 false）
- billing_gate_mode（off/warn/block，P0 默认 warn）
- active_flag
- created_at
- updated_at

补充说明：
- P0 仅启用家族滞在、工作签证（技人国）2 个高频模板，先验证模板生成资料清单、校验、提交包的主链路闭环

---

## 3.9 DocumentRequirement（资料项：材料要求）

资料项是“要什么”，不是“文件本身”。附件上传只会追加版本到 DocumentFileVersion。

### 关键字段
- id
- case_id
- category（客户资料/扶养者资料/事务所内部等）
- item_name
- item_code（模板内唯一 key）
- required_flag
- provided_by_role（主申请人/扶养者/事务所内部等）
- assignee_user_id（责任人，可选）
- due_date（可选：补件/截止）
- status（见下）
- client_visible_flag（客户是否可见）
- client_action_required（是否需要客户处理）
- latest_version_id（可选：便于查询，但提交包不直接引用它）
- last_reminded_at（可选）
- review_comment_latest（可选：最近一次审核意见）
- waive_reason_latest（可选：最近一次豁免原因）
- created_at
- updated_at

### 状态枚举建议（与 05 对齐）
- not_sent（未发送）
- waiting_upload（待提交）
- uploaded_reviewing（已提交待审核）
- approved（审核通过）
- revision_required（需补正）
- waived（无需提供）
- expired（已过期需重交）

---

## 3.10 DocumentFileVersion（资料附件版本）

每次上传生成一条不可变版本；审核结果也应写在版本上，避免“同一文件版本被覆盖式修改”。

### 关键字段
- id
- requirement_id
- version_no（从 1 递增）
- file_name
- storage_key_or_url（存储引用）
- mime_type
- file_size
- sha256（用于去重/防篡改，可选）
- uploaded_by_type（internal_user/client_user）
- uploaded_by_id
- uploaded_at
- visible_scope（internal_only/client_visible）
- review_status（pending/approved/rejected）
- review_by（可选）
- review_at（可选）
- review_comment（可选）
- expiry_date（可选：证件有效期或材料时效）

---

## 3.11 CommunicationLog（沟通记录）

### 关键字段
- id
- case_id
- channel_type（电话/邮件/面谈/IM 等）
- direction（inbound/outbound）
- subject（可选）
- content_summary（必填：用于时间线快速浏览）
- content_detail（可选：富文本/结构化）
- visible_scope（internal_only/client_visible）
- next_action（可选：沟通后的下一步动作）
- next_action_due_at（可选）
- created_by
- created_at

---

## 3.12 Task（任务）

任务用于驱动工作推进与工作台聚合，来源可以是模板、催办、校验失败、补正通知、续签提醒等。

### 关键字段
- id
- case_id
- title
- description（可选）
- task_type（催办/资料审核/文书制作/校验修复/补正/续签跟进等）
- assignee_user_id
- priority
- due_at（可选）
- status（todo/doing/done/canceled）
- source_type（template/requirement/validation/submission/reminder/manual）
- source_id（可选）
- completed_at（可选）
- created_at
- updated_at

---

## 3.13 CaseDeadline（期限）

用途：把“在留到期/补件截止/预约日/结果预计日”等统一建模，便于提醒与日历视图。

### 关键字段
- id
- case_id
- deadline_type（residence_expiry/supplement_due/submission_due/appointment_date/result_expected 等）
- due_at
- source_type（case/manual/requirement/submission 等）
- source_id（可选）
- note（可选）
- created_at
- updated_at

---

## 3.14 Template（文书模板）

### 关键字段
- id
- template_name
- case_type
- doc_type
- language
- version_no
- content_body
- variables_schema（JSON：变量字典与格式化规则）
- active_flag
- created_by
- created_at
- updated_at

---

## 3.15 GeneratedDocumentVersion（生成文书版本）

建议把文书也做成“版本不可变”的产物：每次生成/修改产出新版本，提交包引用具体版本。

### 关键字段
- id
- case_id
- template_id
- title
- version_no
- doc_status（draft/reviewing/final/exported）
- final_flag
- client_downloadable_flag
- output_format（docx/pdf 等）
- storage_key_or_url
- generated_by
- generated_at
- approved_by（可选）
- approved_at（可选）

---

## 3.16 ValidationRun（提交前校验）

用途：把“门槛（Gate）”落地为可执行记录，区分硬性阻断与软性提示，并沉淀报告供复核/提交包引用。

### 关键字段
- id
- case_id
- ruleset_ref（规则集标识/版本，可用 JSON）
- result_status（pending/failed/passed）
- blocking_count
- warning_count
- report_payload（JSON：明细项列表、字段路径、建议修复）
- executed_by
- executed_at

---

## 3.17 ReviewRecord（双人复核）

触发：校验通过后进入待提交前；驳回必须给出原因并回退到合适阶段/状态。

说明：
- ReviewRecord 属于“按策略启用”的能力；并非所有案件、所有模板都默认要求存在

### 关键字段
- id
- case_id
- validation_run_id
- decision（approved/rejected）
- comment（驳回原因/备注）
- reviewer_user_id
- reviewed_at

---

## 3.18 SubmissionPackage（提交包：一次对外提交快照）

用途：固化“某一次对外提交”的快照，保证审计成立，避免历史被覆盖。

### 关键字段
- id
- case_id
- submission_no
- submission_kind（initial/supplement）
- submitted_at
- validation_run_id
- review_record_id（可选）
- authority_name（受理机关/窗口，可选）
- receipt_file_url_or_key（回执/凭证，可选）
- related_submission_id（可选：补正包关联原包）
- created_by
- created_at

---

## 3.19 SubmissionPackageItem（提交包锁定引用）

用途：锁定“这一次提交用到了哪些资料版本/文书版本/关键字段快照”。

### 关键字段
- id
- submission_package_id
- item_type（document_requirement/document_file_version/generated_document_version/field_snapshot）
- ref_id（被引用对象 id；field_snapshot 可为空）
- snapshot_payload（JSON：关键字段快照/摘要，field_snapshot 使用）
- created_at

---

## 3.20 BillingPlan / PaymentRecord（收费与回款）

### BillingPlan 关键字段
- id
- case_id
- milestone_name（例如：签约/提交前/结果后）
- amount_due
- due_date（可选）
- status（unconfirmed/due/paid/waived/refunded 等，按业务裁剪）
- invoice_status（not_required/requested/issued）
- gate_effect_mode（none/warn/block，P0 默认 warn）
- remark（可选）
- created_at
- updated_at

### PaymentRecord 关键字段
- id
- billing_plan_id
- case_id
- amount_received
- received_at
- payment_method
- receipt_file_url_or_key（可选）
- recorded_by
- created_at

---

## 3.21 Reminder/Notification（提醒与触达）

提醒既包括内部待办提醒，也包括客户触达记录（邮件/短信/IM）。建议把“计划提醒”与“实际发送记录”统一在一张表，或拆分为 ReminderRule（P1/P2）+ NotificationLog（MVP）。

补充说明：
- P0 应优先支持内部提醒与站内触达；`email/sms/im` 等外部渠道可作为可选扩展，不应成为首版主链路依赖

### 关键字段（MVP 统一模型）
- id
- case_id（可选：也可能来自客户在留到期）
- target_type（case/customer/requirement/deadline/billing_plan 等）
- target_id
- remind_at
- recipient_type（internal_user/client_user）
- recipient_id
- channel（email/sms/im/in_app）
- template_code（可选）
- dedupe_key（同一对象同一时间窗避免重复发送）
- send_status（pending/sent/failed/canceled）
- retry_count（默认 0）
- sent_at（可选）
- payload_snapshot（JSON：当次消息内容快照，可选）
- created_at

---

## 3.22 AuditLog（审计日志）

建议审计日志作为全对象通用能力，至少覆盖：资料项状态变更、附件版本审核、案件阶段流转、文书定稿、校验通过/失败、复核、提交包生成、收费节点确认。

### 关键字段
- id
- object_type
- object_id
- action_type
- before_data（JSON）
- after_data（JSON）
- operated_by
- operated_at
- ip_address（可选）
- user_agent（可选）

---

## 3.23 User / Role / ClientUser（用户与角色）

### Role 建议（内部）
- super_admin
- office_manager
- lawyer_or_scrivener
- assistant
- sales
- finance

### ClientUser 建议（客户门户）
- client_user（绑定 customer_id 或 contact_person_id，并具备可见范围控制）

---

## 4. 关键关系说明

- 所有主实体建议都携带 `org_id/workspace_id`，先天满足租户隔离与后续多组织扩展
- 一个 Group 可以关联多个 Customer 与 Case，用于表达归属维度（团队/项目组），支撑筛选、协作与可见性口径
- 一个 Lead 可转化为一个 Customer（或匹配既有 Customer）并可创建一个或多个 Case
- 一个 Customer 可以对应多个 Case；Case 默认继承 Customer.group_id 作为归属快照，若发生转组应记录审计日志并保留变更原因
- 若启用企业主数据：一个 Company 也可以对应多个 Case；否则工作类案件的雇主/受入机构信息建议先沉淀在 Case.employer_* 字段
- 一个 Case 可以有多个 CaseParty，用于表达家庭/关键关系；门槛校验依赖其完整性
- 一个 Case 可基于 CaseTemplate 生成多条 DocumentRequirement 与默认 Task
- 一个 DocumentRequirement 可以有多个 DocumentFileVersion；提交包只能引用具体版本
- 一个 Case 可以有多个 GeneratedDocumentVersion；提交包只能引用具体版本
- 一个 Case 可以有多条 ValidationRun 与 ReviewRecord；提交包应锁定当次引用的校验报告与复核结果
- 一个 Case 可以有多个 SubmissionPackage；补正属于新的 SubmissionPackage，并关联原包
- 一个 Case 可以有多个 CaseDeadline，并由 Reminder/Notification 产生提醒留痕
- BillingPlan 与 PaymentRecord 关联到 Case，并可与阶段/门槛联动（如提交前必须满足某收费节点）

---

## 5. 后续扩展建议（非 MVP）

- WorkflowRule（自动化规则引擎：触发器/分派/升级）
- ValidationRuleset（校验规则配置化与版本治理）
- NotificationTemplate（多语言消息模板与变量字典）
- CaseTimelineEvent（统一时间线事件聚合：阶段/沟通/提交/回款等）
- Organization / Workspace（多租户）
