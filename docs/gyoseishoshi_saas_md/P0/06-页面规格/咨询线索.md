# 咨询线索（P0 版）

> 规则引用：Lead 对象语义、去重规则、归属继承链 → [03-业务规则 §2](../03-业务规则与不变量.md#2-对象边界与不可变规则)
> 流程引用：咨询录入与转客户/转案件 → [04-核心流程 §4.1](../04-核心流程与状态流转.md#41-咨询转案件)
>
> **P0 范围声明**：本文定义 P0 首版咨询线索模块的页面规格。P0 只覆盖签约前的最小跟进闭环，不做销售漏斗分析、自动分配、外呼系统集成。
>
> 主目录当前暂无独立的“咨询线索”页面规格；P0 首版以本文为准。

| | |
|---|---|
| **页面目标** | 承接签约前的咨询信息，完成跟进、去重提示、签约后转客户/转案件 |
| **面向角色** | 销售/前台、主办人、助理 |
| **入口** | 左侧主导航「咨询线索」；仪表盘快捷入口「新建线索」 |
| **MVP / P1 边界** | P0：线索录入、基础跟进、状态管理、转客户/转案件、基础去重提示；P1：自动分配、漏斗报表、渠道分析、批量导入导出 |

---

## 0. 结构化速查（人 / AI 共用）

| 维度 | 内容 |
|---|---|
| **页面主对象** | `Lead` |
| **页面主动作** | 录入线索、跟进推进、去重提示、转 `Customer` / `Case` |
| **直接依赖** | 去重规则、归属继承链、`Lead.status`；转化后案件进入 `S1` |
| **上游权威** | [03-业务规则](../03-业务规则与不变量.md)、[04-核心流程](../04-核心流程与状态流转.md)、[07-数据模型](../07-数据模型设计.md) |
| **本页不负责** | 不定义客户/案件字段；不补销售漏斗规则；不做自动分配 |

---

## 1. 典型任务

- **录入新咨询**：登记联系人、来源、意向业务、归属 Group、下一步动作。
- **持续跟进**：把线索从新咨询推进到跟进中 / 待签约。
- **识别重复线索**：用电话/邮箱提示是否已存在客户或线索。
- **签约后转化**：生成 Customer，并创建首个 Case。

## 2. 列表页

### 2.1 字段

- 线索编号
- 联系人姓名
- 电话 / 邮箱
- 意向业务类型
- 来源 / 介绍人
- 当前状态
- 负责人
- 所属 Group
- 下一步动作
- 下次跟进时间
- 最近更新时间

### 2.2 默认筛选

- 当前状态
- 负责人
- 所属 Group
- 意向业务类型
- 跟进时间范围

### 2.3 批量动作

- 批量指派负责人
- 批量调整下次跟进时间
- 批量标记状态

## 3. 详情页

### Tab 1：基础信息

- 线索编号
- 姓名
- 电话 / 邮箱
- 来源 / 介绍人
- 意向业务类型
- 归属 Group
- 负责人
- 备注

### Tab 2：跟进记录

- 跟进时间线（电话 / 邮件 / 面谈 / IM）
- 每条记录至少包含：摘要、结论、下一步动作、下次跟进时间
- 支持一键把跟进结论转成任务

### Tab 3：转化信息

- 去重提示结果（匹配到的 Lead / Customer）
- 转化目标：创建 Customer、创建 Case
- 转化后回填 Customer / Case 跳转入口

### Tab 4：日志

- 状态变更日志
- 负责人变更日志
- Group 变更日志

## 4. 关键动作

| 动作 | 触发条件 | 结果 | 权限 |
|------|---------|------|------|
| 新建线索 | 手动录入 | 创建 Lead，状态为 `new` | 销售、主办人、助理 |
| 记录跟进 | 线索已存在 | 更新时间线、下一步动作与跟进时间 | 销售、主办人、助理 |
| 调整状态 | 跟进推进 | 状态变更为 `following` / `pending_sign` / `signed` / `lost` | 销售、主办人、助理 |
| 转客户 | 已签约或需正式建档 | 创建 Customer，默认继承 Lead.group | 主办人、助理 |
| 转案件 | 已签约且信息足够 | 创建 Case，回填 converted_case_id | 主办人、助理 |
| 调整 Group | 需改归属组 | 变更 Group 并记录原因 | 主办人（需权限） |

## 5. 状态与异常

- **空状态（无线索）**：提示“录入第一个咨询”。
- **重复提示**：按电话 / 邮箱优先提示已有 Lead / Customer，支持人工确认继续。
- **已流失**：保留历史，不进入案件统计。
- **已签约未转化**：在线索列表中高亮，提醒补完成 Customer / Case 创建。

## 6. 权限与可见性

| 角色 | 可见范围 | 可编辑范围 | 可导出 |
|------|---------|-----------|--------|
| 管理员 | 全所线索 | 全部字段 | 需留痕 |
| 主办人 | 本组线索 + 本人负责线索 | 全部业务字段 | 不默认开放 |
| 助理 | 本组线索 | 基础信息、跟进记录、转化辅助 | 不可 |
| 销售/前台 | 本组线索 | 基础信息、跟进记录 | 不可 |

可见性口径受三维访问控制约束（角色 × Group × 负责人/协作者），详见 [03 §10](../03-业务规则与不变量.md#10-权限与可见性)。

## 7. 规则与流程引用

| 引用内容 | 权威文档 |
|----------|---------|
| Lead 对象语义 | [03 §2.1](../03-业务规则与不变量.md#21-p0-核心对象) |
| 归属继承链（Lead→Customer→Case） | [03 §2.2](../03-业务规则与不变量.md#22-归属继承链) |
| 去重规则 | [03 §2.6](../03-业务规则与不变量.md#26-去重规则) |
| 线索状态 | [03 §3.6](../03-业务规则与不变量.md#36-线索状态) |
| 咨询转案件流程 | [04 §4.1](../04-核心流程与状态流转.md#41-咨询转案件) |

---

## P0 明确不做

| 不做项 | 后置到 | 原因 |
|--------|-------|------|
| 漏斗报表 | P1 | 首版聚焦转化闭环，不做分析中心 |
| 自动分配规则 | P1 | P0 以人工指派为主 |
| 批量导入 / 导出线索 | P1 | 首版先跑新线索 |
| 外呼、短信、IM 集成 | P1 | P0 不依赖外部触达系统 |
