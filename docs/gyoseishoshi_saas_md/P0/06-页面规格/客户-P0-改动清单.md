# customers.html P0 保留 / 删除 / 降级 清单

> 本文由 T01（规格映射与删减清单）产出。
> 权威规格：[客户.md](./客户.md)（P0 版）。
> 目标文件：`packages/prototype/admin/customers.html`。

---

## 图例

| 标记 | 含义 |
|------|------|
| **保留** | 与 P0 规格一致，原样保留 |
| **调整** | 保留但需修改文案 / 结构 / 行为 |
| **删除** | 超出 P0 范围，须从页面移除 |
| **降级** | 功能本身保留，但禁用或隐藏入口 |
| **新增** | P0 规格要求但现有页面缺失 |

---

## 1. 页头区域（Header）

| 现有元素 | 行号参考 | 处置 | 说明 |
|----------|---------|------|------|
| H1 "客户" | 797 | **保留** | — |
| 副标题 "P0 以个人客户建档为主…" | 798 | **保留** | 准确描述 P0 范围 |
| "导出（需留痕）" 按钮 | 801 | **删除** | P0 明确不做批量导出（规格 §P0 明确不做） |
| "添加客户" 按钮 | 802–804 | **保留** | 对应 P0 §4 新建个人客户 |

---

## 2. 控制区域（Controls）

| 现有元素 | 行号参考 | 处置 | 说明 |
|----------|---------|------|------|
| 数据范围分段控件（我的 / 本组 / 全所） | 813–817 | **保留** | 匹配 P0 §6 权限可见性分层 |
| 客户类型分段控件（个人 / 企业(P1)） | 818–821 | **删除** | P0 不引入企业客户，保留 disabled 标签仍暗示企业能力，应彻底移除 |
| 搜索框（客户名 / フリガナ / 电话 / 邮箱） | 824–827 | **保留** | 匹配 P0 §1 典型任务"查找客户" |
| 筛选：所属分组 | 831–836 | **保留** | P0 §2.2 默认筛选 |
| 筛选：负责人 | 837–842 | **保留** | P0 §2.2 默认筛选 |
| 筛选：活跃案件 | 843–847 | **保留** | P0 §2.2 默认筛选 |
| "保存视图" 按钮 | 848 | **删除** | P0 规格无此能力；高级视图管理属 P1+ |
| "重置" 按钮 | 849 | **保留** | 标准 UI，辅助筛选 |

---

## 3. 列表表格

### 3.1 表头列

| 现有列 | 行号参考 | 处置 | P0 §2.1 对应 |
|--------|---------|------|-------------|
| 全选 checkbox | 893–901 | **保留** | 批量操作前提 |
| 客户（合并列：名称 + badges + tags + 联系方式） | 904 | **调整** | 见 §3.2 |
| 活跃案件（数字） | 905 | **保留** | 当前活跃案件数 |
| 最近联系 | 906 | **保留** | 最近联系时间 |
| 负责人 | 907 | **保留** | 当前负责人 |
| 所属分组 | 908 | **保留** | 所属分组 |
| 操作 | 909 | **调整** | 见 §3.4 |
| — | — | **新增** | フリガナ —— P0 §2.1 要求的独立列，当前未单独展示 |
| — | — | **新增** | 介绍人/来源 —— P0 §2.1 要求的独立列，当前缺失 |

### 3.2 "客户" 合并列内元素

| 子元素 | 处置 | 说明 |
|--------|------|------|
| 头像圆圈 + 客户名称 | **调整** | 名称保留；超链接改为纯文本或禁用（P0 无详情页承接） |
| Badge "个人" | **删除** | P0 只有个人客户，标记冗余 |
| Badge "重点" | **删除** | P0 无标签 / 重点标记能力 |
| Tag chips（家族滞在 / 需催办 / 技人国 / 在留到期 / 待建案） | **删除** | P0 无标签筛选，属 P1 高级搜索与标签 |
| Tag popover（+N 展开） | **删除** | 随 tags 一并删除 |
| 副行信息：CUS 编号 | **保留** | 客户编号 |
| 副行信息：国籍 | **删除** | 列表中不单独展示；保留为客户基础信息字段 |
| 副行信息：电话 / 邮箱 | **保留** | 辅助搜索确认 |

### 3.3 表格示例数据

三行示例数据的结构需随列调整同步更新，确保展示フリガナ列和介绍人/来源列。

### 3.4 行操作列

| 子元素 | 处置 | 说明 |
|--------|------|------|
| "查看" 按钮（眼睛图标 → data-menu-open） | **删除** | P0 无详情页 / 抽屉，保留会误导验收 |
| "为该客户建案" 按钮（+ 图标） | **保留** | P0 §4 一键建案 |
| "更多" 按钮（三点图标 → 弹出菜单） | **调整** | 菜单内容收敛见下 |

### 3.5 行右键 / 更多菜单

| 菜单项 | 处置 | 说明 |
|--------|------|------|
| "查看客户" | **删除** | P0 无详情页；当前仅 Toast 占位 |
| "为该客户建案" | **保留** | P0 §4 一键建案 |

> 如果菜单只剩一项"为该客户建案"，可考虑移除"更多"按钮，把建案直接作为唯一行操作。

---

## 4. 批量操作条

| 现有元素 | 行号参考 | 处置 | 说明 |
|----------|---------|------|------|
| "已选择 N 条" + "清除" | 861–865 | **保留** | 标准选择状态 |
| 批量指派负责人（select + 应用） | 868–877 | **保留** | P0 §2.3 |
| 批量调整分组（select + 应用） | 878–887 | **保留** | P0 §2.3（需留痕） |
| "批量导出（需留痕）" 按钮 | 888 | **删除** | P0 明确不做 |

---

## 5. 分页

| 现有元素 | 行号参考 | 处置 | 说明 |
|----------|---------|------|------|
| 分页条 "显示 1-3 / 上一页 / 下一页" | 1076–1082 | **保留** | 标准分页 |

---

## 6. 新建客户弹窗

### 6.1 弹窗骨架

| 现有元素 | 行号参考 | 处置 | 说明 |
|----------|---------|------|------|
| 弹窗标题 "新建客户" | 1091 | **保留** | — |
| 关闭按钮 | 1092–1094 | **保留** | — |
| 模式切换分段控件（快速建档 / 家庭建档向导） | 1100–1103 | **删除** | P0 只有一种路径，无需 tab 切换 |
| P0 范围提示文案 | 1104–1106 | **调整** | 移除关于"企业主数据为 P1/P2 能力"的多余文字，改为简洁的弹窗引导 |

### 6.2 快速建档字段（改为唯一路径"新建个人客户"）

| 字段 | 行号参考 | 处置 | P0 §3 Tab1 对应 |
|------|---------|------|-----------------|
| 去重提示区域 | 1110–1123 | **保留** | P0 §5 重复客户提示 |
| 识别名（对内显示） | 1127–1129 | **保留** | 识别名/姓名 |
| 所属 Group * | 1131–1137 | **保留** | 归属 Group（必填） |
| 姓名（法定）* | 1143–1145 | **保留** | 识别名/姓名（必填） |
| 假名（片假名） | 1147–1149 | **保留** | フリガナ |
| 性別 | 1155–1159 | **保留** | 性别 |
| 生年月日 | 1162–1163 | **保留** | 生日 |
| 国籍 | 1166–1168 | **保留** | 国籍（客户基础主数据） |
| 电话 * | 1173–1175 | **保留** | 联系方式 |
| 电话/邮箱去重提示 | 1175 | **保留** | 去重基础 |
| 邮箱 | 1178–1179 | **保留** | 联系方式 |
| 住所 | 1184–1186 | **删除** | 非 P0 新建字段（详情级维护） |
| 来源 / 介绍人 | 1190–1192 | **保留** | 介绍人/来源 |
| 联系语言 | 1195–1200 | **删除** | 非 P0 字段 |
| 头像 | 1205–1206 | **保留** | 写真/头像 |
| 备注 | 1209–1211 | **保留** | 备注 |

### 6.3 家庭建档向导

| 现有元素 | 行号参考 | 处置 | 说明 |
|----------|---------|------|------|
| 整个 `#customerModeFamily` 面板 | 1215–1345 | **删除** | P0 不做家庭建档向导 |
| Step 指示器 / 咨询人表单 / 办理对象卡片 / 在留信息面板 | 1216–1344 | **删除** | 同上 |

### 6.4 弹窗底部按钮

| 现有元素 | 行号参考 | 处置 | 说明 |
|----------|---------|------|------|
| "取消" | 1350 | **保留** | — |
| "上一步" | 1351 | **删除** | 随家庭向导一并删除 |
| "保存草稿" | 1352 | **降级** | 若保留，仅服务个人客户草稿（决策项 D1） |
| "创建客户" | 1353 | **保留** | — |

---

## 7. Toast 提示

| 现有元素 | 行号参考 | 处置 | 说明 |
|----------|---------|------|------|
| Toast 容器 | 1358–1363 | **保留** | 通用 UI 组件 |
| "查看客户（示例）"消息 | JS 2208 | **删除** | 随查看客户入口一并删除 |
| "家庭建档完成（示例）"消息 | JS 1904 | **删除** | 随家庭建档一并删除 |
| 其他 Toast 消息 | — | **调整** | 确保无"家庭"相关文案残留 |

---

## 8. 脚本（`<script>`）清理

### 8.1 需删除的逻辑块

| 逻辑块 | 行号参考 | 原因 |
|--------|---------|------|
| `modeFamilyBtn` 及 mode 切换 `setMode('family')` | 1373, 1424–1435, 1917–1922 | 家庭模式不再存在 |
| `familyStep` 状态机 / `setFamilyStep` | 1437–1462 | 家庭向导步骤控制 |
| `updateFooterButtons` 中 family 分支 | 1464–1473 | 家庭模式按钮状态 |
| `applicantSeq` / `createApplicantCard` / `fillApplicantCard` / `resetApplicants` / `isApplicantCardValid` / `validateApplicants` | 1475–1595 | 办理对象卡片体系 |
| `updateCreateEnabled` 中 family 分支（step 1/2/3） | 1607–1633 | 家庭模式校验 |
| `serializeState` 中 family 分支 | 1697–1735 | 家庭模式序列化 |
| `applyState` 中 family 分支 | 1760–1786 | 家庭模式反序列化 |
| `handleCreate` 中 family 分支（step 推进 + 在留校验） | 1882–1906 | 家庭模式创建流程 |
| `saveDraftBtn` 中 family 分支 | 1951–1981 | 家庭草稿保存 |
| `familyPrevBtn` 事件绑定 | 1924 | 家庭向导上一步 |
| `familyAddApplicantBtn` 事件绑定 | 2011–2016 | 添加办理对象 |
| `familyApplicants` 事件委托（click/input/change） | 2018–2035 | 办理对象卡片交互 |
| family 相关 DOM 引用声明 | 1390–1404 | 失效引用 |
| `bulkExportBtn` 事件绑定 | 2143–2149 | 批量导出 |

### 8.2 需调整的逻辑块

| 逻辑块 | 行号参考 | 调整内容 |
|--------|---------|---------|
| `updateCreateEnabled` quick 分支 | 1598–1604 | 删除 family 分支后可简化为唯一校验路径 |
| `serializeState` | 1678–1695 | 删除 family 分支后只保留 quick |
| `applyState` | 1737–1758 | 同上 |
| `handleCreate` | 1870–1881 | 删除 family 分支后简化 |
| `saveDraftBtn` click handler | 1926–1950 | 若保留草稿，只保留个人客户逻辑 |
| `renderDraftRow` | 1800–1861 | 移除 `displayType: '家庭'` 相关分支 |
| 右键菜单 HTML 和事件 | 2176–2214 | 移除"查看客户"菜单项 |
| `#new` hash 入口 | 2216–2227 | 移除 `setMode('quick')` / `setFamilyStep(1)` / `resetApplicants()` 调用 |

### 8.3 需保留的逻辑块

| 逻辑块 | 说明 |
|--------|------|
| 弹窗 open/close | 基础弹窗行为 |
| `updateDedupeHint` | P0 去重提示 |
| 草稿 CRUD（getDrafts/setDrafts/upsertDraft/removeDraft）| 若决策保留草稿 |
| 全选 / 批量状态更新 | P0 批量操作前提 |
| 批量指派负责人 | P0 §2.3 |
| 批量调整分组 | P0 §2.3 |
| Tag popover 逻辑 | 随 tag 删除一并移除后**不再需要** |

---

## 9. CSS 清理

| 样式 | 处置 | 说明 |
|------|------|------|
| `.segmented-control` / `.segment-btn` | **保留** | 数据范围分段控件仍在使用 |
| `.tag-group` / `.tag-popover` | **删除** | 列表 tag 移除后无使用者 |
| `.row-quick-action` | **保留** | 建案按钮 hover 显示仍需要 |
| `.table-menu` | **调整** | 若只剩建案一项可简化或删除 |
| 其余通用样式 | **保留** | — |

---

## 10. 待决策项

| ID | 问题 | 建议 | 影响任务 |
|----|------|------|---------|
| D1 | 草稿功能是否保留？ | 建议保留，但只服务个人客户草稿。移除家庭类草稿路径和 `displayType: '家庭'` 标记。旧 localStorage 中可能存在家庭草稿，加载时需兼容或清理。 | T05, T06 |
| D2 | 行操作菜单简化为单按钮？ | 若菜单只剩"建案"一项，建议去掉"更多"三点按钮，直接保留行内建案按钮即可，减少一次点击。 | T04 |
| D3 | 客户名称超链接去向？ | P0 无详情页。建议改为纯文本，不可点击。如后续 P0 追加详情抽屉，再恢复。 | T03 |
| D4 | フリガナ和介绍人/来源是独立列还是合并展示？ | 规格 §2.1 明确为独立字段，建议各占独立列。若列过多，フリガナ可合并入客户名称列下方副行；介绍人/来源必须独立可见。 | T03 |

---

## 11. 联动文件影响

| 文件 | 处置 | 说明 |
|------|------|------|
| `admin-prototype.html` | **T07 联动** | 确保首页文案不再出现"家庭建档""企业客户"等超范围描述 |
| `case-create.html` | **T07 最小联动** | 内含另一套新建客户弹窗，本轮不重构，仅做文案避错（如有必要） |

---

## 12. 删减动作汇总（按执行顺序对照后续任务）

| 后续任务 | 删除 | 新增 | 调整 |
|---------|------|------|------|
| T02 页头 | "导出"按钮、客户类型分段控件、"保存视图"按钮 | — | 副标题文案保留 |
| T03 筛选与表格 | badges（个人/重点）、tags、tag popover、国籍副行 | フリガナ列、介绍人/来源列、累计案件数列 | 客户名称链接→纯文本 |
| T04 操作 | "查看"按钮、"查看客户"菜单项、批量导出按钮 | — | 更多菜单简化 |
| T05 弹窗 | 家庭建档向导面板及 tab、住所/联系语言字段、"上一步"按钮 | — | 模式切换→移除，保留国籍字段，弹窗标题可改为"新建个人客户" |
| T06 脚本 | family 状态机、applicant 体系、family 序列化/反序列化、family 校验、bulkExport | — | 草稿逻辑瘦身、右键菜单简化 |
| T07 联动 | — | — | admin-prototype.html 文案对齐、#new 入口验证 |
