# REGRESSION-GATE.md 骨架模板

> 使用说明：复制本模板到 `packages/prototype/admin/{module}/REGRESSION-GATE.md`，按模块实际情况填充。

---

```markdown
# {模块名}模块回归验收门槛

> 权威来源：[P0-CONTRACT.md](./P0-CONTRACT.md) + [{页面规格}.md](../../../../docs/gyoseishoshi_saas_md/P0/06-页面规格/{页面规格}.md)
>
> 用途：后续实现阶段（UI 迁移、脚本拆分、跨模块连通）的**硬性验收门槛**。每轮实现提交前必须逐项确认本清单对应范围全部通过，否则不可合入。
>
> 标记说明：
> - `[原型]` — 原型中可直接视觉/交互验证
> - `[生产约束]` — 生产代码实现时需满足，原型仅在文档中记录
> - `[跨模块]` — 涉及其他模块的一致性

---

## Gate 1 — {空状态场景名}

验收场景：{场景描述}

| # | 验收项 | 标记 | P0-CONTRACT 引用 |
|---|--------|------|-----------------|
| 1.1 | {验收项} | `[原型]` | §{章节} |
| 1.2 | {验收项} | `[生产约束]` | §{章节} |

**通过标准**：1.1–1.N 全部可在原型中视觉确认。

---

## Gate 2 — {全部完成/结清场景名}

...

---

## Gate N — 返回规则

验收场景：用户在不同入口进入模块后，返回行为符合预期，不丢失上下文。

### NA. {入口 A}

| # | 验收项 | 标记 | P0-CONTRACT 引用 |
|---|--------|------|-----------------|
| NA.1 | {导航/跳转/返回验收项} | `[原型]` | §{章节} |

### NB. 跨模块动作文案一致性

| # | 验收项 | 标记 | P0-CONTRACT 引用 |
|---|--------|------|-----------------|
| NB.1 | {文案一致性验收项} | `[跨模块]` | §{章节} |

---

## 验收流程

### 每轮实现提交前

1. **自检**：开发者按 Gate 1–N 逐项确认自己的实现范围
2. **回归**：至少覆盖当前实现范围涉及的所有 Gate 项
3. **标记**：在本文档中标注已通过的项（`[x]`），未通过的项必须修复后再提交

### 最终验收（全部实现完成后）

1. 本文档全部 `[原型]` 项通过
2. `P0-CONTRACT.md` 拆分回归清单全部通过
3. `split-manifest.json` 的 `regressionChecklist` 全部通过
4. `npm run fix` + `npm run guard` 通过

### Gate 失败处理

- 任何 Gate 中有 `[原型]` 项未通过：该轮实现 **不可合入**，修复后重新验收
- `[生产约束]` 项在原型阶段可标记为 `[deferred]`，但必须在 `MIGRATION-MAPPING.md` 中有对应的生产实现说明
- `[跨模块]` 项若受限于其他模块未就绪，标记为 `[blocked: 模块名]`，待对方就绪后补验

---

## 与现有文档的关系

| 文档 | 职责 | 本文档补充 |
|------|------|-----------|
| `P0-CONTRACT.md` §{状态章节} | 列出 N 种状态的规格要求与原型处理方式 | 将每种状态拆解为可逐项打勾的验收用例 |
| `P0-CONTRACT.md` 拆分回归清单 | 按功能区域组织的完整回归列表 | 按**场景**组织，聚焦 N 个关键风险场景 |
| `split-manifest.json` regressionChecklist | 机器可读的回归项（平铺列表） | 补充场景维度的验收结构和 Gate 通过标准 |
| `MIGRATION-MAPPING.md` | 原型→生产的代码映射 | 为 `[生产约束]` 项提供可追溯的实现指引 |
```
