# T02 完成报告 — billing admin 基础骨架与共享契约冻结

## 完成状态

**通过** — `npm run fix` + `npm run guard` 全量通过（check:deps / typecheck / lint / jsdoc:lang:check / test / build）。

## 产出物

### 1. 核心类型 `types.ts`

冻结 17 个类型/接口：

- 枚举：`BillingStatus`, `NodeStatus`, `PaymentRecordStatus`, `CollectionSkipReasonCode`, `GroupCode`, `BillingSegment`, `GateEffectMode`
- 筛选：`BillingFiltersState`, `BillingStatusFilter`, `BillingGroupFilter`, `BillingOwnerFilter`
- 摘要：`BillingSummaryData`, `BillingSummaryCardDef`, `SummaryCardVariant`
- 列表行：`CaseBillingRow`, `BillingNextNode`
- 回款流水：`PaymentLogEntry`
- 收费计划：`BillingPlanNode`, `BillingPlanDetail`
- 弹窗：`RegisterPaymentFormFields`
- 批量催款：`CollectionResult`, `CollectionResultDetail`
- 风险确认：`RiskAcknowledgement`
- 通用：`SelectOption`, `StatusOption`

### 2. Demo 数据 `fixtures.ts`

- 5 条案件收费行，覆盖全部 4 种回款状态（overdue / partial / due / paid）
- 5 条回款流水，覆盖 3 种记录状态（valid / voided / reversed）
- 5 份收费计划详情
- 摘要统计数据
- 风险确认记录、批量催款结果
- 状态/Group/Owner/跳过原因等选项常量
- 分段视图定义、摘要卡定义、排序优先级

### 3. 总装壳 `BillingListView.vue`

最小组装视图，导入所有占位子组件，不含业务逻辑。

### 4. 占位组件（8 个）

| 组件 | 实现任务 |
|------|---------|
| BillingSummaryCards.vue | T03 |
| BillingFilters.vue | T03 |
| BillingTable.vue | T04 |
| BillingBulkActionBar.vue | T04 |
| BillingPagination.vue | T04 |
| PaymentLogTable.vue | T05 |
| PaymentModal.vue | T05 |
| BillingToast.vue | T05 |

### 5. Model Hooks（5 个）

| Hook | 实现任务 |
|------|---------|
| useBillingFilters.ts | T03 |
| useBillingSelection.ts | T04 |
| useBillingToast.ts | T05 |
| usePaymentModal.ts | T05 |
| useRiskAckLog.ts | T05 |

### 6. 边界契约 `CONTRACT.md`

定义 T03/T04/T05 各自允许修改和禁止触碰的文件清单。

## 验收检查

- [x] `packages/admin/src/views/billing/` 目录存在且结构稳定
- [x] `types.ts` 与 `fixtures.ts` 能支持后续并行任务
- [x] `BillingListView.vue` 只是总装壳
- [x] 组件和 model 的命名、路径、导入规则已明确
- [x] 后续任务无需再创建共享文件，只需填充各自组件与 hooks
- [x] `npm run guard` 全量通过
