# W07 执行摘要

## 结果

- 已新增 `plan/mempalace/deployment-topology.md`
- 已冻结 sidecar 与主仓库的隔离原则：仓库内存文档，仓库外跑 `MemPalace`
- 已明确四类目录：`runtime`、`data`、`logs`、`backups`
- 已把活动数据目录与备份目录分离，确保故障时可恢复且不污染主仓库

## 依据

- `W07` 任务要求 sidecar 独立部署，避免污染 `cms-client` 主栈
- `W02/W03` 的边界前提是第一期只读知识检索，不接真实客户正式数据
- `W08/W15` 后续需要依赖稳定的目录拓扑来定义环境回退和运维恢复流程

## 自检

- `MemPalace` 与业务主系统隔离关系：已说明
- 数据目录与备份目录归属：已说明
- sidecar 故障不会阻塞主系统：已说明
- 未把 sidecar 并入 `npm` 主流程：已满足

## 阻塞

无。当前未发现必须改动仓库现有运行方式或 CI 配置才能成立的前提。
