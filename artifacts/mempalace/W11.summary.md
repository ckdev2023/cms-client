# W11 执行摘要

## 结果

- 已新增 `plan/mempalace/index-baseline-plan.md`
- 已把首轮全量索引拆成 `B0-B7` 七个固定批次，并明确 `B1-B5` 为最小必跑基线
- 已明确 `_output` 必须排在全部 `L1` 权威来源之后，`_raw` 必须排在 `_output` 之后
- 已补充逐批成功条件、失败分级、补跑上限和放行闸门
- 已收敛 run 级与 batch 级最小基线统计字段，避免把日志细节混入基线主记录

## 依据

- `W11` 要求先定义“先索引什么，再索引什么”，并把失败补跑写得比顺利路径更具体
- `W04` 已冻结 `source_id`、白名单和 `_output/_raw` 预留位，因此批次设计直接绑定 manifest 条目
- `W06` 已冻结 `wing/room` 骨架，因此成功条件可直接检查核心 `room` 覆盖
- `W08` 与 `W07` 已冻结环境和备份边界，因此补跑方案采用批次快照恢复，而不是混入业务仓库流程

## 自检

- 权威源先于 `_output/_raw`：已满足
- `_raw` 被安排在最后：已满足
- 失败只允许同批补跑、不跨层推进：已明确
- 基线统计仅保留决策必需字段：已满足

## 阻塞

无。本文假设 `W04`、`W06`、`W08` 的冻结结论继续有效；若这些前置文档后续被回改，需要同步回看本方案。
