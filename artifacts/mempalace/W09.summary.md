# W09 执行摘要

## 结果

- 已新增 `plan/mempalace/mcp-readonly-scope.md`
- 已冻结第一期 `MemPalace` MCP 只读开放面为 7 个工具：`search_knowledge`、`get_document`、`get_citation_context`、`list_indexed_sources`、`route_query`、`ground_query`、`prepare_grounded_answer`
- 已明确 diary、drawer、memory 写入、索引控制、恢复回滚、通用文件系统读取均不进入一期范围
- 已把“一期不是通用只读文件浏览，而是受白名单约束的检索 + 门禁接口”写成硬边界

## 依据

- `W09` 任务要求第一期只做检索增强，不允许通过 MCP 回写或修改记忆内容
- `W02` 已冻结白名单来源，因此 MCP 读取范围必须被索引白名单约束，而不是放开通用路径读取
- `W03` 已冻结红线与脱敏边界，因此即使技术上能读，也不能把敏感内容通过 MCP 暴露出去
- `W07` 已冻结 sidecar 与仓库的隔离边界，因此客户端只能读索引结果，不能碰 `runtime/data/logs/backups`

## 自检

- 允许开放的只读工具：已明确
- 禁止开放的写入工具：已明确
- 每类工具的风险理由：已说明
- 通用 `read_file` / `list_dir` 式能力误入一期：已排除

## 阻塞

无。`W08` 的环境与版本文档尚未落地为权威文件，但其缺失不影响一期“只读不写”能力边界冻结。
