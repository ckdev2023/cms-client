# W14 执行摘要

## 结果

- 已新增 `plan/mempalace/eval-report-template.md`
- 已冻结评估报告的最小结构：元信息、总体汇总、分组结果、单题记录、失败/误导复盘、`Go/No-Go`
- 已明确区分 `miss` 与 `misleading`，避免把“没答到”和“答到了但误导”混成一类
- 已把来源层级、回链质量和协议合规纳入单题记录字段
- 已补充红线判定与放行范围写法，可直接支撑 `Go/No-Go` 评审

## 依据

- `W14` 要求模板能记录命中率、误导率、回链质量和失败案例
- `W12` 已提供题目 ID、分组和成功信号，因此模板应直接按题记录
- `W13` 已冻结成功/降级协议，因此模板必须把协议合规与来源层级纳入评分
- 来源优先级已固定为 `L1 > L2 > L3`，因此评估不能只看“命中”，还要看是否越权

## 自检

- 模板能记录命中率和误导率：已满足
- 模板能记录来源层级和回链质量：已满足
- 模板能直接汇总到 `Go/No-Go` 评审：已满足
- 已明确区分“未命中”和“命中但误导”：已满足

## 手工试填

- 试填 1：`AQ-S08`
  结果：可记录为 `hit`，`backlink_layer=L3`，`protocol_compliance=pass`，因为该题的正确行为本来就是降级回答。
- 试填 2：`AQ-H04`
  结果：若答案把 `_output` 当最终权威且不给 `L1` 回链，可记录为 `misleading`，并触发红线；符合预期。

## 阻塞

无。当前 `W12` 问题集和 `W13` 回答协议已稳定，足以支撑统一评估模板落地。
