# W01 执行摘要

## 结果

- 已新增 `plan/mempalace/source-priority.md`
- 已冻结三级来源优先级：`L1(P0/P1/事务所流程) > L2(_output) > L3(_raw)`
- 已定义同主题冲突时的固定裁决顺序，并按 `P0 / P1 / 场景资料` 三类问题分别给出优先级矩阵
- 已明确 `_raw` 只能作为待编译线索，不能直接作为最终结论
- 已规定回答时必须展示来源层级标记，并补充正反例

## 依据

- `P0/99`：明确 P0 是首版权威源、`raw` 不可直接执行、结构化导航不得变成新规则来源
- `P1/01`：明确 P1 只承接扩展，不改写 P0 底座
- `_output/00-outputs.md`：明确 `_output` 是可回灌结论，不是长期权威层
- `_raw/00-inbox.md`：明确 `_raw` 只追加、待编译
- `docs/事务所流程/*`：明确其角色更接近场景流程与资料输入

## 自检

- 三级来源优先级：已覆盖
- 冲突裁决顺序：已覆盖
- `_output` / `_raw` 角色差异：已覆盖
- `_raw` 不可直接充当权威结论：已覆盖
- 共享文件 `plan/tasks/MP-README.md`：未改动

## 阻塞

无。当前未发现必须新增第四层来源或必须改写现有权威文档的情况。
