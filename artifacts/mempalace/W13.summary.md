# W13 执行摘要

## 结果

- 已新增 `plan/mempalace/answer-protocol.md`
- 已冻结 `MemPalace` 的最小回答协议：先判断能否定案，再决定如何回答
- 已明确来源层级展示规则，要求最终结论优先回链 `L1` 权威文档
- 已明确 `_output` 只能做桥接层、`_raw` 只能做补充证据或待编译线索
- 已补充冲突、缺失、仅有 `_raw`、仅有 `_output` 时的固定降级说法

## 依据

- `W13` 明确要求先定义失败时怎么答，再定义成功时怎么答
- `W01` 已冻结 `L1 > L2 > L3` 的来源优先级，回答协议必须继承该顺序
- `W12` 已给出验收问题集，因此可直接抽取问题做协议演练
- `_output` 与 `_raw` 的角色边界已在现有计划中固定，不能混成同一层

## 自检

- 协议明确要求来源层级展示：已满足
- 协议明确要求优先回链权威文档：已满足
- 协议明确 `_raw` 只能作为补充证据：已满足
- 未扩展到评估模板、MCP 开放策略或客户端实现：已满足

## 手工演练

- 问题 1：`AQ-S04` “如果 _output 中有一条关于状态机的整理结论，而 P0 已有冻结规则，最终应以谁为准？”
  结果：协议要求直接以 `P0` 定案，并把 `_output` 降级为桥接层；符合预期。
- 问题 2：`AQ-S08` “若某结论只能在 _raw 中找到，而 L1 没有冻结定义，系统应该怎样回答？”
  结果：协议要求输出“待编译 / 待确认”，且只能标记 `[L3:_raw]`；符合预期。
- 问题 3：`AQ-H04` “为什么 _output 不能长期充当最终权威来源？”
  结果：协议要求把 `_output` 明确定位为桥接层，并优先回链 `L1`；符合预期。

## 阻塞

无。当前前置来源优先级与验收问题集已存在，足以稳定落下回答协议文档。
