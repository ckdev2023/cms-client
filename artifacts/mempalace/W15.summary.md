# W15 执行摘要

## 结果

- 已新增 `plan/mempalace/runbook.md`
- 已把值班 runbook 收敛为统一手册，覆盖备份、增量更新、全量重建、回滚和故障处理
- 已明确故障分类 `R1-R6`，并固定“先保留证据、能回滚先回滚”的处置顺序
- 已把全量重建流程对齐 `W11` 的 `B0-B7` 固定批次，避免跨层补跑
- 已把回滚、健康检查、值班记录和结束检查清单写成可直接执行的编号步骤

## 依据

- `W15` 要求值班者不依赖历史聊天即可执行，因此 runbook 采用动作化、编号化写法
- `W08` 已冻结 release、快照、升级与回退边界，因此 runbook 直接继承“先备份、再切换、失败优先回滚”
- `W11` 已冻结首轮全量索引的批次顺序和补跑规则，因此全量重建必须按 `B0-B7` 执行，且失败时不跨批推进
- `deployment-topology.md` 已冻结仓库外运行目录、数据目录、日志目录和备份目录，因此 runbook 只在这些目录内操作

## 自检

- 包含全量重建流程：已满足
- 包含增量更新流程：已满足
- 包含备份、回滚和故障处理流程：已满足
- 值班者无需翻历史聊天即可执行：已满足

## 阻塞

无。当前 runbook 依赖的拓扑、环境和索引基线文档均已存在，可直接作为值班手册使用。
